#!/usr/bin/env node
/**
 * `neo-agent` bin 엔트리 — composition root (`docs/CLI-INTERFACE.md` §2).
 *
 * T-001 스캐폴딩 시점의 자리표시자다. 시작 시퀀스 8단계 배선은 T-008에서 채운다.
 */

console.log("neo-agent 0.1.0 (scaffolding — T-008에서 배선)");
