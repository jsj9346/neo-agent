# 문서 지위 선언 규약

**`neo-agent-main/docs/*.md` 머리가 무엇을 선언해야 하는가의 정본.**

- 상태: 구현 완료
- 근거: scripts/check-doc-status.mjs
- 작성일: 2026-08-13
- 최종 개정: 2026-08-13(§3.5 — 근거 하나 폐기 / §6.1 — 수를 뺄 때의 부칙 / §3.2 — 본문 조건은 별건 위임 / §5.1 — 표 대조 계약 확정 / §5.2 — 경계 셋 확정, 표 대조 구현 착지 / §1·§5 — 인용 형식은 `DOC-CITATION.md`로 분리·배정, §6.2 — 누적 총계 부칙, §8 U-d 닫힘 / §2.2·§7 — 누적 총계 처분, §6.2 자기 적용) · 2026-08-14(§5 — `PROVIDERS.md` 행 추가 / §5.3 — 역방향 검사 계약 확정 / §8 U-a 닫힘) · 2026-08-14(§5.3 — 「자리」를 두 층으로 가름: 집합 술어는 순수, 순회·라벨은 실행부. 구현 착지) · 2026-08-14(§6 — «규범적 수» 예외 추가, 판별은 수 하나 단위) · 2026-08-17(머리 — 불변 열거에서 수를 빼고 갈래를 §포인터로 든다 / §4 — 출력 행 서술 정정 / §5.1 — 머리 인용 갱신 / §8 U-c — 「최종 개정」 완전성 판정 추가)

> **이 문서는 자기 자신의 첫 시험 사례였고, 그 시험이 실제로 발동했다.** 이 줄은 2026-08-13에 `구현 전`으로 작성됐다(`945fe4f^`) — 그때는 `근거:`가 가리키는 게이트 스크립트가 없었기 때문이다. 스크립트를 작성한 직후 게이트를 처음 돌리자 **이 문서가 `anchor-present`(§4 ⑦)로 잡혔다.** 다른 문서들이 전부 `missing`(①)이던 첫 실행에서 유일하게 다른 위반이었고, 그것이 예고한 대로 이 줄을 `구현 완료`로 바꾸게 했다. 설계 주석(`scripts/doc-status.mjs`)이 말한 *"구현이 착지하는 순간 게이트가 깨진다"*가 이 문서에서 먼저 일어났다.

**불변(계약)**: 머리 필드의 이름과 값 유니온(§3), 게이트가 판정하는 실패 갈래(머리 판정 §3.1 · 표 대조 §5.1 · 역방향 §5.3), 파서가 관대해지지 않는다는 것(§3.3).
**조정 가능(세부)**: 실패 메시지 문구, 탐색 줄 수 상한, 스크립트의 파일 위치.

이 문서는 `plans/20260813-docs-headers-verify-report.md` U-1(미규정)의 해소이자, `ARCHITECTURE.md` 머리가 *"이 금지를 검사하는 수단이 없었고 지금도 없다"*고 자백한 자리에 수단을 놓는 첫 조각이다.

전제는 `SAFE-DEFAULTS.md` 머리와 같다: **여기 있는 어떤 것도 보안 경계가 아니다.** 이 게이트는 문서가 조용히 거짓이 되는 것을 막는 장치이고, 그 이상을 주장하지 않는다.

---

## 1. 경계 — 이 규약이 다루는 것과 다루지 않는 것

| 다룬다 | 다루지 않는다 |
|---|---|
| `neo-agent-main/docs/*.md`의 **머리 지위 선언** | 본문 각 절이 코드와 일치하는가 (`K-002` — §8 U-b) |
| 선언된 상태 ↔ **실물 경로의 존재/부재** | 실물의 *내용*이 문서와 맞는가 |
| 선언 형식의 기계적 강제 | `최종 개정:` 이력, 문체 |
| — | **본문이 다른 자리를 가리키는 주소의 형식** — `DOC-CITATION.md`(2026-08-13 신설) |
| 루트 `docs/`(레퍼런스 분석) 밖 — **우리 설계 문서만** | `packages/*/test/`·`.claude/`·`plans/`의 머리 |

**예외 목록을 두지 않는다.** `neo-agent-main/docs/` 안의 `.md`는 전부 대상이다. 예외를 허용하면 예외 목록 자체가 다음 부패 후보가 되고, 이 레포는 이미 그 실패를 겪었다 — `SANDBOX.md` 머리의 2026-08-12 정정이 손으로 유지한 6개 문서 목록이 그것이다. 그 목록은 정확했지만, 정확했다는 것이 **사람이 한 번 더 세어 확인해야만** 알 수 있었다.

---

## 2. 왜 필드여야 하는가 — 세 부패가 각각 다른 것을 요구했다

2026-08-13의 전수 대조가 세 유형을 한 사이클에 냈다(`plans/20260813-docs-headers-verify-report.md`). 원인이 다르므로 처방도 달라야 하고, 아래 설계는 셋을 각각 다른 수단으로 막는다.

| 부패 | 실물 | 이 규약이 막는 방식 |
|---|---|---|
| **F-1** — 6개 문서가 `구현 전`인데 패키지가 존재 (7일·4일) | 안 건드려서 낡음 | **게이트가 매 `pnpm check`마다 대조한다.** `구현 전` + 앵커 존재 = 위반(§4 ⑦) |
| **F-2** — `구현 완료(검색 제외)`가 구현 5일 뒤 개정에도 생존 | 건드렸는데도 낡음 | **괄호 한정을 표현할 수 없게 만든다.** 값은 닫힌 유니온이고 파서는 정확 일치만 받는다(§3.3) |
| **F-3** — 수 서술 금지가 같은 파일 안에서 두 번 깨짐 | 금지가 있는데도 발생 | **금지의 예외를 명문화한다**(§6). 검사 수단 없는 규칙은 규칙이 아니라 의도의 기록이다 |

### 2.1 F-2가 가장 무겁고, 검사로는 못 잡는다

F-1은 검사가 잡는다. F-3은 예외를 명문화하면 판정이 끝난다. **F-2만 다르다** — `구현 완료(검색 제외)`는 그 자체로 참인 문장이었고, 검색이 구현되면서 조용히 거짓이 됐다. 어떤 대조 규칙을 세워도 괄호 안의 자연어를 실물과 맞출 수는 없다.

그래서 이 설계는 F-2를 **검사하지 않고 표현 불가능하게 만든다.** 값이 세 개뿐인 닫힌 유니온에는 "단 X는 예외"를 적을 자리가 없다. 한정이 필요하면 본문에 쓰고, 머리는 전체에 대해 참인 한 값만 든다.

이것이 `ARCHITECTURE.md` §2.2(컴플라이언스를 타입 레벨에서 강제)와 설계 원칙 4(**불가능한 상태는 타입으로 배제**)를 문서 형식에 적용한 것이다.

### 2.2 필드 파서는 자기오염에 걸리지 않는다 — 이것이 grep 격자와 갈리는 지점

이 레포는 **자기오염을 되풀이해 겪었다**(2026-08-12 수 서술 격자 · 08-13 crontab 주석 · 08-13 문서 머리 감사 · 08-13 플랜의 검증 조건 — `devnotes/20260813-devnote.md` §15.4). 구조는 매번 같다 — **어떤 것을 설명하는 텍스트가 그 검사 대상 문자열을 포함하고**, 부재·패턴을 찾는 grep이 거기 걸린다. 정정 주석이 옛 문면을 되살리는 것이 가장 흔한 형태이고, 설명 주석이 **왜 이것을 안 쓰는지**를 적으며 그 이름을 부르는 것도 같은 구조다. `devnotes/20260813-devnote.md` §11의 처방은 *"검사는 파일이 아니라 **주장이 사는 절 범위**에 걸어야 한다"*였다.

**필드는 그 처방의 가장 강한 형태다 — 주장이 사는 범위가 한 줄로 축소된다.** 정정 주석이 옛 상태를 아무리 인용해도 파서는 `- 상태:` 로 시작하는 비인용 줄만 읽는다(§3.3). 부재를 찾는 grep과 달리, 필드 파서는 **무엇을 읽을지 자신이 정한다.**

이것이 10사이클의 *"격자를 하나 더 만들지 않는다"* 판정과 충돌하지 않는 이유이기도 하다. 그 판정이 거부한 것은 **문자열 패턴을 하나 더 늘리는 것**이고, 이 설계는 패턴을 늘리지 않는다 — 문서가 스스로 신고하는 필드를 하나 만들고 그것만 읽는다.

---

## 3. 계약 — 머리 필드

### 3.1 타입

파일에 적히는 형태는 한국어 필드다. 아래는 파서가 만드는 값이다.

```typescript
/** `neo-agent-main/` 기준 상대 경로. 파일 또는 디렉터리 하나. */
type AnchorPath = string;

/**
 * 문서 머리가 선언하는 지위. 닫힌 유니온이며, 값에 한정·괄호·마크업을 붙일 수 없다.
 * `근거:` 줄의 존재 여부가 kind에 의해 완전히 결정된다 — 옵셔널이 아니다.
 */
type DocStatus =
  /** `- 상태: 구현 완료` + `- 근거: <경로>` — 앵커가 존재해야 한다 */
  | { readonly kind: "implemented"; readonly anchor: AnchorPath }
  /** `- 상태: 구현 전`   + `- 근거: <경로>` — 앵커가 부재해야 한다 */
  | { readonly kind: "not-yet"; readonly anchor: AnchorPath }
  /** `- 상태: 구현 주장 없음` — `근거:` 줄을 두면 위반 */
  | { readonly kind: "no-claim" };

type Violation =
  | "missing"          // ① `상태:` 줄이 없다
  | "duplicate"        // ② `상태:` 줄이 둘 이상이다
  | "unknown-value"    // ③ 유니온 밖의 값 (여기서 F-2의 괄호 한정이 잡힌다)
  | "anchor-required"  // ④ implemented|not-yet 인데 `근거:` 가 없다
  | "anchor-forbidden" // ⑤ no-claim 인데 `근거:` 가 있다
  | "anchor-missing"   // ⑥ implemented 인데 앵커 경로가 없다
  | "anchor-present";  // ⑦ not-yet 인데 앵커 경로가 있다  ← F-1 6건이 여기

/**
 * 판정의 산출. 성공 갈래는 `status`를 **통째로** 든다 — `kind` + 옵셔널 `anchor`로
 * 평탄화하면 `{kind:"no-claim", anchor:"x"}`가 타입상 합법이 되어 위 불변("`근거:`의
 * 유무가 kind에 의해 완전히 결정된다")이 판정 경계에서 풀린다.
 *
 * **문서 이름은 이 타입에 없다.** 판정 함수는 소스 텍스트와 앵커 존재 여부만 받으므로
 * 파일명을 원리적으로 만들 수 없다 — 게이트 루프가 파일명과 짝지어 출력한다.
 * (2026-08-13 명문화: 최초 문면은 두 갈래 모두에 `doc: string`을 뒀는데(`97a88b9^`),
 * 그것은 순수 판정에서 만들 수 없는 값이었다. QA 독립 검증이 잡았다.)
 */
type Verdict =
  | { readonly ok: true; readonly status: DocStatus }
  | { readonly ok: false; readonly violation: Violation; readonly detail: string };
```

### 3.2 세 값이 무엇을 뜻하는가

| 값 | 뜻 | `근거:` |
|---|---|---|
| `구현 완료` | 이 문서의 계약이 실물로 존재한다 | **필수** — 존재해야 하는 경로 |
| `구현 전` | 계약은 확정됐고 실물은 아직 없다 | **필수** — *아직 없어야 하는* 경로 |
| `구현 주장 없음` | 이 문서는 구현 상태를 주장하지 않는다 | **금지** |

`구현 전`이 앵커를 드는 것이 이 설계의 핵심이다. **앵커 없는 `구현 전`은 검사할 수 없고, 검사할 수 없는 선언이 정확히 F-1이 7일간 산 방식이다.** 구현이 착지하는 순간 게이트가 ⑦로 깨지므로, 머리를 고치는 것이 구현 커밋의 일부가 된다 — 나중에 기억해서 하는 일이 아니게 된다.

`구현 주장 없음`은 *"본문에 구현 이야기가 없다"*는 뜻이 아니라 **이 문서에는 단일 앵커로 검사할 대상이 없다**는 뜻이다. 2026-08-13 전수 대조에서 `상태:` 선언이 아예 없던 문서들이 *"주장이 없으면 거짓일 수 없다"*는 근거로 적합 판정을 받았고(판정: `plans/20260813-docs-headers-verify-report.md` · 그 문면: `devnotes/20260813-devnote.md`), 그 상태를 값으로 승격한 것이다. **어느 문서가 이 값을 갖는지는 §5 표가 정한다** — 여기에 목록을 두지 않는다(§6.1).

**본문의 구현 상태 주장은 이 값의 배정을 무효화하지 않는다.** 그것은 이 규약이 아니라 `K-002`(본문 격자)의 대상이다 — §1이 그은 경계 그대로다.

> **판정 (2026-08-13) — 본문 조건은 *배정 조건*이 아니라 *별건 위임*이다.** 개정 전 문면은(`92e45b3^`) *"이 값을 고른 문서는 **본문 어디에서도 구현 상태를 주장하지 않아야** 하며"*였고, 이것이 배정의 **전제조건**으로 읽혔다. `ARCHITECTURE.md`가 그 읽기에서 막혔다 — §5 표가 이 문서에 `구현 주장 없음`을 배정했는데 본문(:132)이 구현 완료를 주장하므로, 배정 조건으로 읽으면 배정 자체가 위반이 된다. 그 문서 머리가 *"판정 필요"*를 걸어 둔 채 표를 따랐다.
>
> 배정 조건으로 읽지 않는 근거는 **§3.5와 같다**: 게이트는 머리 40줄만 읽으므로 *"본문 어디에서도"*를 원리적으로 검사할 수 없다. 배정 조건으로 두면 **검사할 수 없는 조건이 값의 정의에 들어가고**, 그것을 어긴 상태는 아무 데도 나타나지 않는다(§2.6 위반). `설계 확정`을 필드에서 뺀 이유가 `구현 주장 없음`에 되살아나는 셈이다. 근거: `plans/20260813-doc-status-body-verify-report.md` W-2.

**이 값이 도피처가 되는 것은 §5.1의 표 대조가 막는다.** 배정 권한은 §5 표에 있고, 게이트가 그 표를 읽어 **배정 ↔ 실제 머리**를 맞춘다. 어떤 문서가 머리를 `구현 주장 없음`으로 바꾸고 `근거:` 줄을 지우면 표와 어긋나 «값 어긋남»으로 깨진다.

> **2026-08-13 정정 — 이 문단은 원래(`832d44a^`) *"지금은 아무것도 막지 않는다"*였다.** 그때는 참이었고, 같은 날 §5.1이 계약을 세우고 구현이 착지하며 거짓이 됐다. 그 구멍을 들고 있던 §8 U-f는 이 커밋으로 닫힌다.

### 3.3 파서는 관대하지 않다

| 규칙 | 근거 |
|---|---|
| 탐색 범위는 파일 **첫 40줄** | 머리는 머리에 있다. 본문 인용까지 읽으면 자기오염이 되살아난다 |
| 줄이 `- 상태: ` 로 **정확히** 시작 — 선행 공백·`>` 인용·들여쓰기 불가 | 정정 주석은 `>` 블록이므로 자동 배제된다(§2.2) |
| 값은 순수 텍스트 **정확 일치**. `**구현 완료**` 는 위반 | 관대한 파서는 `구현 완료(검색 제외)`도 통과시킨다. 그것이 F-2다 |
| `상태:`·`근거:` 는 **각각 자기 줄**을 갖는다 | §3.4 |
| `상태:` 줄은 파일당 **정확히 1개** | 0개(미선언)와 2개 이상(모호)을 각각 다른 위반으로 잡는다 |

### 3.4 필드를 한 줄에 몰지 않는 이유 — F-2의 진짜 원인

`CLI-INTERFACE.md`의 머리는 `945fe4f` 이전에 개정 이력 8건을 나열한 뒤 마지막에 `상태:`를 붙인 **한 줄**이었다(그 커밋이 그것을 필드로 갈랐다). **`(검색 제외)`가 5일간 살아남은 것은 그 괄호가 그 줄의 끝에 있었기 때문이다** — 같은 줄의 다른 부분을 고치면서 눈이 거기까지 가지 않았다.

그래서 각 필드는 자기 줄을 갖는다. 이것은 미관이 아니라 **F-2의 인과에 직접 대응하는 수정**이다.

`최종 개정:` 이력은 이 규약이 규정하지 않는다(§8 U-c). 다만 `상태:`·`근거:` 와 같은 줄에 둘 수 없다.

### 3.5 `설계 확정`을 필드에서 뺀다

개정 전에는 **일부** 문서 머리가 `상태: 설계 확정, **구현 완료**` 형태로 두 축을 한 줄에 들었다(`945fe4f`에서 필드로 교체). **`설계 확정`은 필드에서 뺀다.**

**근거는 하나다 — 대조할 실물이 없다.** 이 규약이 검사할 수 있는 것은 선언 ↔ **실물 경로의 존재/부재**뿐이고(§1), `설계 확정`에는 그 경로가 없다. 필드로 두면 **아무도 검사하지 않는 필드**가 하나 생기며, 검사받지 않는 선언이 조용히 거짓으로 사는 것이 정확히 F-1의 형태다(앵커 없는 `구현 전` — §3.2). 이 규약의 원칙은 **선언은 기계가 검사할 수 있을 때만 의무**다.

> **2026-08-13 정정 — 이 절이 근거를 하나 더 들고 있었고, 그것이 두 번 썩었다.** 원래의 둘째 근거는(`92e45b3^`) *"`neo-agent-main/docs/`에 놓인 문서는 **예외 없이 전부** 설계가 확정된 것이라 이 축의 변별력이 0"*이었다. 두 가지가 틀렸다. ①**실측이 반증한다** — `945fe4f^` 시점 문서 16개 중 이 축을 든 머리는 8개였고, 그중 `설계 확정`이 아닌 값을 든 것이 1개였다(`ARCHITECTURE.md` — *"설계·구현 병행"*). 나머지 8개는 축 자체가 없었다. 값이 갈렸으므로 "변별력 0"이 아니다. ②**기대는 규칙이 존재하지 않는다** — *"설계 중인 것은 애초에 여기 있지 않다"*는 `neo-agent-main/docs/`의 배치 규칙을 인용하는데, 그 규칙은 이 문장 말고 어디에도 명문화돼 있지 않다(전수 grep 무소득). 자기가 만든 전제를 자기가 인용하고 있었다.
>
> 그래서 둘째 근거를 **되살리지 않고 폐기했다.** 첫째 근거만으로 결론이 서고, 폐기하면 이 절이 외부 상태에 의존하지 않게 된다 — 카드가 하나 늘 때마다 조용히 거짓이 되던 문장이 사라진다. 근거는 `plans/20260813-doc-status-body-verify-report.md` W-1·W-3.

---

## 4. 게이트 — 무엇을 검사하는가

- 자리: **`neo-agent-main/scripts/check-doc-status.mjs`**(2026-08-13 신설). `pnpm check`가 부른다.
- **판정과 실행부는 파일이 다르다.** 순수 판정(§3·§3.3)은 부작용 없는 `scripts/doc-status.mjs`에 살고, 위 파일은 순회·앵커 확인·출력만 맡는다. 한 파일이면 계약 테스트가 판정 함수를 임포트하는 것만으로 게이트 전체가 돌고, 실물이 레드인 날엔 `process.exit(1)`이 테스트 워커를 죽여 **"계약 위반"이 "테스트 파일이 사라짐"으로 나타난다**(2026-08-13 QA 발견 · 선례는 `package-boundary.contract.test.ts`가 예산 게이트를 임포트 대신 텍스트로 읽는 것). `import.meta.main` 가드는 쓰지 않는다 — 그 속성은 Node 24.2.0에서 들어왔고 `engines`는 `>=24`라 24.0~24.1에서 게이트 본문이 통째로 건너뛰어진다. **침묵 통과는 이 게이트가 존재하는 이유 그 자체이므로** 런타임 조건 대신 파일 경계로 가른다.
- 예산 게이트(`check-core-budget.mjs`)에 합치지 **않는다** — 그쪽의 검사 대상은 `packages/` 안이고 이것은 `docs/`다. 실패 메시지가 섞이면 "무엇이 깨졌나"가 흐려진다.
- 판정은 §3.1의 7가지. **판정되지 않은 문서는 통과가 아니다** — 예산 게이트가 이미 쓰는 방식(`EXPECTED_CONSISTENCY_NOTES` 대조)을 그대로 따라, 발견한 `.md` 수와 판정한 수가 다르면 그 자체로 실패다.
- 출력은 판정한 문서마다 한 행, 그 뒤에 갈래별 요약 행. 성공도 조용하지 않다 — `ARCHITECTURE.md` §2.6(모든 행동은 가시적 결과로 끝난다).

**게이트가 잡지 못하는 것을 여기 적어 둔다.** 머리 판정(§3.1의 일곱)은 문서가 **스스로 신고한 값**을 실물과 맞추고, 그 값이 §5가 배정한 값인지는 **§5.1의 표 대조**가 따로 묻는다. 남는 것은 앵커의 *내용*이다 — 앵커가 존재한다는 것은 `구현 완료`의 필요조건이지 충분조건이 아니다. 빈 디렉터리도 존재하고, 계약과 무관한 파일도 존재한다. 이 게이트는 **7일간 거짓인 머리**를 잡지 **한 시간 전에 거짓이 된 머리**를 잡지 않는다. 그것을 잡으려면 본문 절 단위 앵커가 필요하고 그것이 `K-002`다.

---

## 5. 문서별 확정 값

**아래 표가 이 절의 정본이고, 문서의 수는 여기서 세지 않는다**(`ARCHITECTURE.md` 머리의 수 서술 금지와 같은 근거 — 손으로 센 수는 문서가 하나 늘면 어긋난다). 표에 없는 `.md`가 `neo-agent-main/docs/`에 있으면 그것은 표의 누락이고, **§5.1의 표 대조가 «표에 없는 문서»로 잡는다.**

> **2026-08-13 정정 — 이 자리가 "게이트가 ①(미선언)로 잡는다"(`2a40c11`)고 적고 있었다.** ①은 `- 상태:` 줄이 **없을 때만** 발동한다. §3을 지켜 머리를 제대로 쓴 새 문서가 표에 없으면 게이트는 그것을 정상 통과시킨다. 실측(2026-08-13): `- 상태: 구현 주장 없음`만 든 탐침 파일을 `docs/`에 두고 게이트를 돌리자 **`17/17건` 통과**했다. 즉 이 문장은 표의 누락이 자동으로 잡힌다는 뜻으로 읽혔지만 잡히는 것은 **머리를 안 쓴 문서**뿐이었다. 근거: `plans/20260813-doc-status-body-verify-report.md` W-2 → §8 U-f.

표의 앵커는 **2026-08-13에 전수 실증했다.** 설계 시점에는 `scripts/check-doc-status.mjs` 하나만 부재였고 그것은 의도된 것이었다(이 문서 자신의 `구현 전` 앵커) — **같은 날 구현되어 지금은 표의 모든 앵커가 실존한다.** 경로는 `neo-agent-main/` 기준이다.

| 문서 | `상태:` | `근거:` |
|---|---|---|
| `APPROVAL-GATE.md` | 구현 완료 | `packages/gate/src` |
| `ARCHITECTURE.md` | 구현 주장 없음 | — |
| `CLI-INTERFACE.md` | 구현 완료 | `packages/cli/src` |
| `COMPACTION.md` | 구현 완료 | `packages/compaction/src` |
| `CORE-INTERFACE.md` | 구현 완료 | `packages/core/src` |
| `DISTRIBUTION.md` | 구현 완료 | `packages/cli/bin/neo-agent.mjs` |
| `DOC-CITATION.md` | 구현 완료 | `scripts/check-doc-citation.mjs` |
| `DOC-STATUS.md` | 구현 완료 | `scripts/check-doc-status.mjs` |
| `MEMORY.md` | 구현 완료 | `packages/memory/src` |
| `PROVIDERS.md` | 구현 완료 | `packages/providers/src` |
| `REUSE-MAP.md` | 구현 주장 없음 | — |
| `SAFE-DEFAULTS.md` | 구현 완료 | `packages/cli/src/config.ts` |
| `SANDBOX.md` | 구현 완료 | `packages/sandbox/src` |
| `SEARCH.md` | 구현 완료 | `packages/store/src/search.ts` |
| `SESSION-STORE.md` | 구현 완료 | `packages/store/src` |
| `TECH-STACK.md` | 구현 주장 없음 | — |
| `TOOLS-INTERFACE.md` | 구현 완료 | `packages/tools/src` |
| `WEB-ACCESS.md` | 구현 완료 | `packages/web/src` |
| `WEB-UI.md` | 구현 전 | `packages/serve` |

**`SAFE-DEFAULTS.md`·`DISTRIBUTION.md`가 파일 앵커를 드는 것에 규칙 분기가 없다.** 앵커는 "존재 여부를 물을 수 있는 경로"이고 디렉터리와 파일이 그 점에서 다르지 않다. 문서 유형별로 규칙을 나누자는 안은 배제했다 — 새 문서마다 *"내 문서는 어느 쪽인가"*가 판정 항목이 되고, 판정 항목은 미뤄진다.

**정정 주석은 이 표와 무관하다.** 머리의 2026-08-13 정정 주석은 `>` 인용 블록이라 파서 밖이며(§3.3), 그대로 둔다 — 과거 기록은 고치지 않는다.

**표의 문서 셀에서 볼드와 괄호 주석을 뺐다**(2026-08-13). `DOC-STATUS.md` 행이 `**`DOC-STATUS.md`** (이 문서)` 형태였는데(`92e45b3^`), 아래 §5.1이 그 자리를 기계가 읽을 대상으로 확정했으므로 §3.3과 같은 이유로 마크업을 허용하지 않는다.

### 5.1 표의 행 문법 — 기계 대조의 계약

**왜 필요한가.** 배정 권한은 §5 표에 있는데(§3.2) 표를 읽는 기계가 없으면 `구현 주장 없음`이 도피처가 된다. 어떤 문서가 머리를 `구현 완료` → `구현 주장 없음`으로 낮추고 `근거:` 줄을 지우면 실물 패키지가 있어도 게이트는 그린이다. 표의 누락(§5 서두 정정)과 이 도피처는 **같은 구멍의 두 방향**이다.

**행 문법.** §5 표의 각 행은 정확히 세 셀이고, 각 셀은 하나만 든다:

| 셀 | 형식 |
|---|---|
| 문서 | 백틱으로 감싼 파일명 하나 — `` `NAME.md` ``. 볼드·괄호 주석 금지 |
| `상태:` | §3.2 유니온의 값 하나. 순수 텍스트 정확 일치 |
| `근거:` | 백틱으로 감싼 경로 하나, 또는 앵커 없음을 뜻하는 `—` |

§3.3과 같은 이유로 **관대하지 않다.** 마크업과 괄호 주석을 허용하면 `구현 완료(검색 제외)`가 머리 대신 표에서 되살아난다 — F-2를 한 층 아래로 옮기는 것뿐이다.

**절 범위에서만 읽는다.** 파서는 `## 5.` 제목과 **그 다음에 처음 나오는 `###` 소제목** 사이만 본다 — `## 6.`까지가 아니다. 이 문서에는 세 셀 표가 여럿이고(§2·§3.2·§3.3·§7), 파일 전체에서 세 셀 행을 긁으면 §7의 레퍼런스 판정표가 문서 목록으로 섞여 들어온다. **§2.2의 처방 그대로다** — 검사는 파일이 아니라 **주장이 사는 절 범위**에 건다.

> **경계를 `###`로 끊는 이유는 이 절 자신이다.** 초안은 `## 5.`부터 다음 `## `까지를 범위로 잡았는데, 그러면 **§5.1(지금 이 소절)의 표들이 범위 안에 든다.** 지금은 그 표들이 두 셀이라 세 셀 행 문법에 안 걸리지만, 나중에 §5.x에 세 셀 표가 하나 생기면 그 행들이 문서 배정으로 섞인다. *"관대하지 않은 파서"*를 정의하는 절이 자기 범위를 관대하게 잡고 있었다.

**§3.1의 일곱 위반을 늘리지 않는다.** 일곱은 *문서 하나의 머리*에 대한 순수 판정이고, 표 대조는 *집합 대 집합*이라 판정 함수가 만들 수 없는 값이다(§3.1이 `doc: string`을 뺀 것과 같은 이유). 그래서 표 → 배정 맵의 **순수 파서만** `doc-status.mjs`에 두고, 대조 자체는 fail-closed 그물과 같은 자리인 **실행부**에 둔다(§4). 머리의 불변 선언은 수를 들지 않고 갈래를 §포인터로 드므로(2026-08-17 개정 · `K-041`), 이 표 대조는 그 열거에 §5.1로 든다 — 일곱을 늘리지 않으면서도 어느 불변에도 안 걸리는 갈래가 되지 않는다.

**실패 갈래 넷** — 라벨을 붙이고 게이트를 깨뜨리는 것은 전부 실행부다. 재료 중 «행 파싱 실패»만 순수 파서가 만들고(행 문법은 문면만으로 정해진다), 나머지 셋은 집합 대조라 실행부가 만든다:

| 갈래 | 뜻 |
|---|---|
| 표에 없는 문서 | `docs/`에 있는데 표가 배정하지 않았다 — **표의 누락**(§5 서두 정정이 실측한 자리) |
| 문서 없는 행 | 표가 배정했는데 그 문서가 없다 — 표의 유령 |
| 값 어긋남 | 표의 `상태:`·`근거:`와 실제 머리가 다르다 — **도피처가 여기서 닫힌다** |
| 행 파싱 실패 | 위 문법에 맞지 않는 행. 관대하게 넘기지 않는다 |

**갈래를 번호로 부르지 않는다.** §3.1이 이미 ①~⑦을 쓰고 있어 번호를 붙이면 두 목록이 grep에서 섞인다. 위 이름이 그대로 게이트 출력의 라벨이다.

### 5.2 경계 셋 — 위 문법이 정하지 않은 자리 (2026-08-13 확정)

행 문법만으로는 정해지지 않는 자리가 셋 있었다. **세 대안이 전부 같은 모양이었다** — 조용히 통과하는 경로를 만든다. 그래서 셋 다 fail-closed 또는 위치 기반으로 확정했다.

| 물음 | 확정 |
|---|---|
| 헤더 행과 구분선 행을 어떻게 배제하나 | **파이프 블록의 첫 두 행은 데이터가 아니다.** 마크다운 표의 정의상 고정 2행이므로 **위치**로 자른다. 셀 내용으로 판별하면(백틱 없는 문서 셀은 건너뛴다) 데이터 행이 백틱을 잃었을 때 그 문서가 **어느 갈래에도 안 걸린다** — 넷을 다 만들고도 구멍이 남는다 |
| 절 범위 안에 표가 둘 이상이면 | **실패.** 배정의 정본이 둘이 되는 상태를 통과시키지 않는다. 첫 표만 읽으면 새 표가 배정을 바꿔도 아무도 모른다 |
| 머리 판정이 이미 실패한 문서를 값 어긋남 대조에 넣나 | **건너뛴다.** 머리 위반이 이미 보고됐고, 같은 원인을 두 줄로 내면 *"고칠 곳이 둘"*로 읽힌다 — 위반 이름이 곧 고칠 곳의 주소라는 것이 이 게이트의 설계다. **단 건너뛴 문서를 출력에 한 줄로 밝힌다**: 이것이 없으면 침묵 경로가 하나 생긴다(§4 · `ARCHITECTURE.md` §2.6) |

**이 표를 두 셀로 두는 것은 미관이 아니다.** 위 인용 블록이 *"지금은 그 표들이 두 셀이라 세 셀 행 문법에 안 걸린다"*고 적었고, 여기에 세 셀 표를 두면 그 문장이 거짓이 된다. 실제 파서는 `###` 경계로 끊으므로 영향받지 않지만, **기각된 대안이 왜 기각됐는지를 말하는 문장이 조용히 거짓이 되는 것**은 이 규약이 막으려는 부패 그 자체다.

**표에 닿지 못한 경우는 갈래가 아니라 fail-closed 그물이다.** 절(`## 5.`)이 없다 · 절 범위에 표가 없다 · 표가 둘 이상이다(위 확정) — **배정의 정본에 닿지 못한 경우는 전부** «행 파싱 실패»와 같은 라벨을 쓰지 않는다. 같은 주소를 주면 *"행 하나가 틀렸다"*와 *"표를 못 읽었다"*가 구별되지 않는다. §4의 그물 둘(문서 0개 · 판정 수 불일치)과 같은 자리·같은 성격이다.

> **2026-08-13 정정 — 이 문단이 *"셋 다"*로 닫혀 있었다.** 성격(*"배정의 정본을 통째로 못 읽은 것"*)으로 정의해 놓고 열거를 수로 닫았는데, 그 성격에 해당하는 그물이 실행부에 하나 더 있었다(배정의 정본 파일 자체의 부재 — 그 주석이 스스로 *"위 그물 둘과 같은 형태다"*라고 밝힌다). **정의가 열려 있으면 열거도 열어 둬야 한다** — §6.1이 *"수를 뺄 때 한정사를 남긴다"*를 말한 것의 역방향이고, 여기는 주어가 집합 전체라 수를 빼도 전칭 함정이 없다. 위 세 항목은 §5.2가 **판정한** 자리를 드는 것이지 그물의 전수가 아니다. 근거: `plans/20260813-doc-status-51-52-verify-report.md` V-1.

> **미규정 — 같은 문서가 두 행에 배정되는 경우.** 위 셋과 함께 정하지 않았고 구현이 마주쳤다. **D-2와 같은 모양의 모호함**(배정의 정본이 둘)이라 그 판정 근거를 그대로 적용해 fail-closed로 두었으나, 이 문서가 정한 바는 아니다 — `scripts/doc-status.mjs`에 `[미규정]`으로 표시돼 있고 판정은 `K-034`(파서의 미규정 갈래)에 속한다.

### 5.3 역방향 — 문서 없는 패키지 (2026-08-14 확정)

**여기까지의 검사는 전부 문서에서 출발한다.** 문서가 신고한 값을 실물과 맞추고(§4), 그 값이 표의 배정과 같은지 묻는다(§5.1). 그러므로 **어느 문서도 들지 않는 실물**은 원리적으로 안 보인다 — 이 규약의 §8 U-a가 든 구멍이다. 실측이 그것을 실물로 보여줬다: `packages/providers`가 2026-08-06에 태어나 2026-08-14까지 정본 문서 없이 살았고, 그동안 이 게이트는 매번 그린이었다.

**같은 계열이 F-1이다** — 문서가 `구현 전`이라 선언하는 동안 패키지가 7일·4일간 존재했던 것(2026-08-13 전수 대조). F-1은 사람 눈으로 발견됐고, 이 방향에는 그때도 지금도 기계가 없었다.

**판정: 건다.** 계약은 다음과 같다.

| 항목 | 확정 |
|---|---|
| 대상 | `neo-agent-main/packages/` 아래 `package.json`을 가진 디렉터리 |
| 「덮였다」 | §5 표 어느 행의 `근거:` 앵커가 그 디렉터리 자신이거나 그 아래 경로다. 한 패키지를 여러 문서가 덮어도 된다(`SESSION-STORE.md`·`SEARCH.md`가 `store`를 그렇게 덮는다) |
| 실패 라벨 | «문서 없는 패키지» — §5.1의 갈래들과 같은 이름 규칙(번호를 붙이지 않는다) |
| 자리 | **두 층으로 가른다** — 「덮였다」의 집합 술어는 **순수**(`doc-status.mjs`)이고, 실물 순회·라벨·종료는 **실행부**(`check-doc-status.mjs`)다 |
| 표에 닿지 못한 경우 | 이 검사도 §5.1 말미의 fail-closed 그물 뒤에 선다. 배정의 정본을 못 읽었으면 덮임 여부를 물을 수 없다 |
| 패키지 0개 | 통과가 아니라 실패. §4의 «문서 0개» 그물과 같은 자리·같은 성격이다 |

**§3.1의 일곱을 늘리지 않는다.** 머리 판정의 일곱은 *문서 하나의 머리*에 대한 순수 판정이고, 이것은 집합 대 집합이다 — `judge()`가 만들 수 없는 값이라는 점에서 §5.1의 표 대조와 같다.

> **§5.1과 자리가 갈리는 이유** (2026-08-14 확정). §5.1은 표 대조를 통째로 실행부에 두었고 근거는 *"`집합 대 집합`이라 판정 함수가 만들 수 없는 값이다"*였다(원문의 기울임을 백틱으로 옮긴다 — D-2가 요구하는 것은 마커의 보존이고, 기울임 안의 기울임은 표기가 성립하지 않는다). 그 근거는 **`judge()`에 대해** 참이지 «순수 함수 일반»에 대해 참은 아니다 — 이 검사의 두 피연산자(앵커 목록·패키지 목록)는 **둘 다 호출자가 넘길 수 있으므로** 집합 술어 자체는 I/O 없이 쓰인다. 그래서 여기서는 술어를 순수로 뽑아 계약 테스트를 붙이고, 디스크를 읽는 부분만 실행부에 둔다. **검사를 늘리면서 그 검사를 아무도 재지 않는 상태를 함께 만들지 않는다는 것**이 이 분할의 값이다. §5.1의 판정은 이 절이 바꾸지 않는다.

**격자를 하나 더 만드는 것이 아니다.** §8 U-d가 기각한 것은 «자유 서술에 문자열 패턴을 하나 더 거는 일»이고 그 기각 근거는 정밀도였다. 여기 두 피연산자는 **둘 다 구조적이다** — 디렉터리 목록과, 게이트가 이미 파싱하고 있는 표의 `근거:` 셀. §2.2가 grep 격자와 필드 파서를 가른 그 구분에서 이것은 **필드 쪽**이고, U-f가 표 대조를 채택한 것과 같은 자리다.

**예외 목록을 두지 않는다**(§1). 어떤 패키지도 「문서 없어도 되는 패키지」로 등록되지 않는다 — 예외 목록 자체가 다음 부패 후보라는 것이 §1의 판정이고, 이 규약은 이미 그 실패를 한 번 겪었다. 패키지가 정본 문서를 가질 수 없는 사정이 실제로 생기면 그것은 예외를 등록할 근거가 아니라 **이 절을 개정할 사유**다.

**순서가 강제된다.** 이 검사가 붙는 순간 문서 없는 패키지는 즉시 레드이므로, **문서와 §5 표 행이 먼저 착지해야 한다.** 이 규약 자신이 반대 순서를 시험했던 전례가 머리의 정정 주석에 있다 — 거기서는 `구현 전`을 선언해 레드를 **예고**했고, 이번에는 예고할 자리가 없다(패키지는 이미 존재한다). `PROVIDERS.md`와 그 표 행은 2026-08-14에 착지했다.

> **이 확정이 만든 미규정은 2026-08-17에 닫혔다.** 2026-08-14에 이 절이 실행부 갈래를 하나 더 얹었을 때, 머리의 불변 선언은 수를 든 문면이라 *어떻게 읽어도* 표 대조·역방향 갈래가 어느 불변에도 걸리지 않았다. 그 자리가 `K-041`이었고 그때는 임의로 고치지 않았다. 처분은 **머리에서 수를 빼고 갈래마다 이름과 §포인터를 드는 것**이다 — 이제 이 절(§5.3)이 머리 열거에 자기 이름으로 든다. 갈래가 또 늘면 머리에 이름을 하나 더 얹으면 되고, 세지 않으므로 남은 항목들이 조용히 거짓이 되지 않는다.

---

## 6. `ARCHITECTURE.md` 머리 수 서술 금지의 예외 명문화

**판정(2026-08-13, 유저): 과거 시점 관측은 금지 밖이다. 다만 예외를 문구에 명문화한다.**

`ARCHITECTURE.md` 머리의 금지를 집행하며 단 정정 주석이 `1284`·`1,944`·`2,099`를 들고 있어 자기 저촉 여부가 열려 있었다. 판정 근거: 그 수치는 **날짜와 근거 리포트 경로가 함께 박힌 과거 관측**이므로 실물이 변해도 거짓이 되지 않는다. 금지가 겨냥한 것은 *"지금 몇인가"*를 주장하는 수이지 *"그때 몇이었나"*를 기록하는 수가 아니다.

미규정으로 두고 이 판단만 내리면 **다음 감사가 같은 것을 또 판정한다.** 그래서 금지 문구 자체를 개정한다:

> **예외 — 과거 시점 관측.** 날짜와 근거(리포트 경로 또는 커밋)가 함께 박힌 정정 주석의 수치는 이 금지 밖이다. 판별 기준은 **시제**다 — 날짜 없이 현재를 주장하는 수는 예외가 아니다. **누적 총계는 날짜가 있어도 예외가 아니다** — 관측 시점의 값이 아니라 그 시점*까지*의 값이라 주장이 현재까지 열려 있고, 사건이 하나 더 나면 어긋난다. 시간 상한을 함께 박아 닫으면(*"…까지 N번"*) 예외 안이다.

예외가 새는 자리가 되지 않도록 판별 기준을 함께 못박았다. `K-025` 사이클의 교훈(*"조건을 하나 더 두면 그 조건이 다음에 새는 자리가 된다"*)이 여기 걸리는데, 그 사이클이 조건 추가를 기각한 이유는 **비용이 0이었기 때문**이다(no-op push가 exit 0). 여기는 다르다 — 예외를 안 두면 정정 주석에서 근거 수치를 전부 빼야 하고, 그러면 *"이 줄은 N일간 거짓이었다"*가 얼마나 어긋났는지를 말할 수 없게 된다. 즉 대안의 비용이 실재한다.

**이 예외는 §4 게이트의 검사 대상이 아니다.** 수 서술 금지의 기계화는 이 문서가 열어 두는 항목이다(§8 U-d).

**판정(2026-08-14, 유저): 규범적 수 — 기계가 재는 수는 금지 밖이다.**

`PROVIDERS.md` §2.1의 *"정확히 2개"*·*"형제는 `core` 하나다"*가 날짜 없는 현재형 수라 금지 문면에 **문자 그대로 걸린다.** 그런데 그 수는 예산 게이트가 `package.json`의 `dependencies`를 **정확 집합 비교**하므로(`scripts/check-core-budget.mjs`) 의존성이 셋이 되는 순간 `pnpm check`가 레드다. 즉 서술적 수(실물을 **센** 값)가 아니라 **규범적 수(계약이 **정하는** 값)**이고, 위 예외의 시제 기준은 이 갈래를 판별하지 못한다. 금지 문구를 다시 개정한다:

> **예외 — 규범적 수.** 어긋나면 `pnpm check`가 레드가 되는 수는 이 금지 밖이다. 금지가 겨눈 실패 모드는 «조용히 거짓이 됨»인데, 기계가 재는 수는 조용히 거짓이 될 수 없다 — 실물이 문서를 앞지르는 순간 게이트가 깨지고, 그 깨짐이 문서를 고치게 만든다. 판별은 **수 하나 단위**다: «이 절의 검사는 게이트가 한다»류의 절 단위 선언은 그 절의 모든 수를 예외로 만들지 않으며, **그 수를 실제로 재는 검사를 지목할 수 없으면 예외가 아니다.**

**이 문서가 만든 갈래가 아니다.** 같은 문면이 `31abb2a`의 `CORE-INTERFACE.md`에 2026-08-11부터 있었고 감사에서 걸린 적이 없다 — **선례가 조용히 서 있었다.** 같은 부류가 더 있다: `CORE-INTERFACE.md` §1의 *"의존성 예산: `zod` 하나"*(게이트 `core` 항이 잰다), `CLI-INTERFACE.md`의 허용 내장 목록(경계 테스트가 부분집합 단언으로 잰다). 판정 없이 두면 다음 감사가 이 셋을 각각 다시 연다.

**«수 하나 단위»가 이 예외의 방어선이다.** `K-025` 교훈(*"조건을 하나 더 두면 그 조건이 다음에 새는 자리가 된다"*)이 여기 걸리는데, **새는 형태가 이미 실물로 관측됐다** — `PROVIDERS.md` §2.1이 *"검사는 예산 게이트(`scripts/check-core-budget.mjs`)가 한다"*고 **절 단위로** 선언하면서 금지 모듈 여덟을 들었고 게이트는 다섯만 쟀다(`plans/20260814-providers-doc-verify-report.md` V-1). 절 단위 면제를 허용했다면 그 절의 모든 수가 근거 없이 예외가 됐을 것이다. 그래서 판별을 수 하나 단위로 못박고 «재는 검사를 지목할 수 있는가»를 요건으로 둔다 — 지목은 기계적이라 감사자가 그 자리에서 규칙을 발명하지 않는다.

**대안의 비용은 실재한다.** §6 본문이 첫 예외를 둘 때 쓴 저울을 그대로 쓴다 — 예외를 안 두면 §2.1 표제에서 값을 빼고 *"워크스페이스 밖 SDK 하나 + 형제 하나"*처럼 써야 하는데, 그 값은 게이트와 대조하는 사람이 실제로 쓰는 값이다. §6.1의 처방(수를 뺄 때 한정사를 남긴다)으로는 그 손실을 메우지 못한다 — 여기서 빠지는 것은 범위가 아니라 **계약값 자체**다.

**이 예외도 §4 게이트의 검사 대상이 아니다**(§8 U-d). 판별이 기계적이라는 것은 «사람이 지목할 수 있다»는 뜻이지 «게이트가 판정한다»는 뜻이 아니다.

### 6.1 부칙 — 수를 뺄 때 한정사를 남긴다 (2026-08-13 추가)

어긋난 수를 **지우는 것**이 이 금지의 통상 처방인데, **주어가 집합의 일부일 때 수를 그냥 빼면 존재 주장이 전칭 주장으로 바뀐다.**

같은 날 §3.5에서 그 일이 실제로 일어났다 — *"현재 6개 문서가 … 든다"*의 수를 빼자 *"문서 머리는 … 들었다"*가 되어, **문서 16개 중 8개**(2026-08-13 실측, `945fe4f^`)에만 해당하던 서술이 **전부에 대한 주장**이 됐다. 고치기 전에는 *수가* 틀렸고 고친 뒤에는 *범위가* 틀렸다(`plans/20260813-doc-status-body-verify-report.md` W-1).

> 이 문단의 수치가 **날짜와 커밋을 함께 든 이유**가 위 예외의 판별 기준이다. 부칙은 정정 주석이 아니므로 예외 문면(*"정정 주석의 수치"*)에 그대로 걸리지 않는다 — 그래서 수마다 시제를 박아 걸릴 필요가 없게 만들었다.

처방은 둘 중 하나다:

| 상황 | 처방 |
|---|---|
| 주어가 집합 **전체** | 수를 빼고 성질만 남긴다 (§5의 «정정 주석은 이 표와 무관하다» 문단이 이 경우 — 모든 정정 주석이 `>` 블록이므로 전칭이 참) |
| 주어가 집합의 **일부** | 한정사(`일부`·`대부분`)를 남기거나, 수를 **날짜 박힌 과거 관측**으로 바꾼다(위 예외) |

부패의 종류가 *수 어긋남*에서 *범위 거짓*으로 바뀔 뿐인 처방은 개선이 아니다. **후자가 더 나쁘다** — 수는 세면 반증되지만 전칭 주장은 반례를 찾아야 반증되고, 아무도 반례를 찾지 않는다.

**이 부칙은 처방이지 금지 문면이 아니다.** `ARCHITECTURE.md` 머리의 금지에 옮겨 적지 않는다 — §6 본문의 예외는 그쪽과 문면이 같아야 하지만, 부칙은 이 규약이 자기 실패에서 얻은 작업 지침이다. 기계화 판정(`K-032`)은 §8 U-d에서 닫혔고 이 부칙은 **기계 밖에 남는다.**

### 6.2 부칙 — 누적 총계는 시간 상한으로 닫는다 (2026-08-13 추가)

위 예외의 시제 기준이 **누적 총계를 다루지 못했다.** 열거는 날짜가 있어 예외처럼 보이고 총계는 본질적으로 현재형이라, 둘이 한 문장에 있으면 판별이 갈리지 않는다.

§2.2의(`6549e93^`) *"이 레포는 **자기오염을 세 번 겪었다**"*가 그 자리다 — 열거된 셋에는 전부 날짜가 박혀 있는데 **총계는 열려 있어** 네 번째가 나는 순간 어긋났고, 실제로 같은 날 어긋났다(`devnotes/20260813-devnote.md` §15.4). 어긋나는 시점이 *"기록이 하나 더 늘어난 때"*라 **아무도 그 문장을 다시 보지 않는다.**

처방은 수를 세는 목적에 따라 갈린다:

| 수의 역할 | 처방 |
|---|---|
| **논거** — 반복성 자체가 주장의 근거다 | 시간 상한을 박아 닫는다: *"2026-08-13까지 네 번"*. 정보를 잃지 않으면서 주장이 닫힌다 |
| **장식** — 성질만으로 주장이 선다 | 수를 뺀다. §6.1 표의 «주어가 집합 전체» 행과 같다 |

**«…까지»는 §6.1이 말한 한정사의 시간축 형태다.** §6.1이 *"수를 뺄 때 한정사를 남긴다"*로 범위를 닫았다면, 여기는 수를 남기면서 **시점**을 닫는다. 둘 다 열린 주장을 닫는 수단이고, V-1이 얻은 격자(*"정의가 열려 있으면 열거도 열어 둬야 한다"*)의 반대편 손잡이다 — 열거를 열 수 없을 때는 정의를 닫는다.

> **조건을 하나 더 두는 것의 비용.** `K-025` 사이클의 교훈(*"조건을 하나 더 두면 그 조건이 다음에 새는 자리가 된다"*)이 여기 걸린다. §6 본문이 예외를 둘 때 쓴 저울을 그대로 쓴다 — 대안(누적 수를 전부 금지)의 비용이 실재하는가? 실재한다. 이 레포의 격자 상당수가 *"같은 형태를 N번 겪었다"*를 근거로 세워졌고, 그것을 못 쓰게 되면 격자의 근거가 사라진다.

---

## 7. 레퍼런스 대비 — 가져온 것이 없다

| 항목 | 판정 | 근거 |
|---|---|---|
| OpenClaw 문서 YAML frontmatter (`summary`·`read_when`·`title`) | **안 함** | 구조화된 머리라는 발상은 같으나 **구현 상태 필드도 문서↔실물 대조도 없다.** 검색·목차 생성이 목적이라 우리 문제를 풀지 않는다 |
| OpenClaw `scripts/package-docs-map.mjs` | **안 함** | 이름과 달리 npm 패키징 시 목차를 생성하는 도구다. 지위 선언과 무관 |
| OpenClaw `scripts/check-deprecated-jsdoc.mjs` | **흔적만** 🧬 | 문서성 주석을 CI에서 검사한다는 **자리**만 가져온다. 대상(JSDoc deprecated 태그)은 우리와 다르다 |
| hermes `AGENTS.md` | **없음** | 문서 상태 규약이 없다. grep 무소득 |

**이 설계에 선례가 없다는 것 자체가 기록할 값이다.** 두 레퍼런스 모두 문서가 코드보다 낡는 문제를 기계로 풀지 않았고, 우리는 그 부패를 8건 실측했다(2026-08-13 전수 대조 — `plans/20260813-docs-headers-verify-report.md`. F-1 여섯 · F-2 하나 · F-3 하나). 재도입 트리거는 없다 — 가져올 것이 없으므로.

---

## 8. 미결 — 이 문서가 정하지 않은 것

- **U-a. (닫힘 — 2026-08-14) `providers`에 정본 문서가 없다 + 역방향 검사.** 두 물음 다 답이 나왔다. ① 전용 정본 문서를 **신설했다**(`PROVIDERS.md`) — `COMPACTION.md` §8이 2026-08-10에 남긴 유보 트리거(*"트리거: providers 계약이 2건 이상 실재하게 될 때 문서 신설을 재론한다"* — 그 문면은 2026-08-14 이관이 지웠다. 앵커 `31abb2a`)가 발동했고, 결정적 근거는 부재가 계약을 지우지 않고 **인용의 고리**로 바꾼다는 실측이었다(`PROVIDERS.md` §4.2 — 세 자리가 같은 계약을 서로 다른 세 곳에 귀속시키는데 셋 다 그 문장을 갖고 있지 않았다). ② **역방향 검사를 건다** — 계약은 §5.3이고, 구현은 같은 날 착지했다(`check-doc-status.mjs` 실행부 + 순수 술어 `uncoveredPackages`).

  **이 미결의 서술이 8일간 낡아 있었다**(2026-08-14 확인). 개정 전 문면은(`31abb2a`) *"계약이 `CORE-INTERFACE` §8 + `SAFE-DEFAULTS` §3 + `ARCHITECTURE` §2.2에 흩어져 있다"*였고 그 열거가 실물과 어긋났다 — `SAFE-DEFAULTS` §3은 그 계약을 **든 적이 없고**(귀속만 받았다), 대신 `COMPACTION.md` §8(컨텍스트 창)과 `DISTRIBUTION.md` §4(버전 정본)가 빠져 있었다. 더 무거운 것은 이 항목이 같은 리비전에서 *"`providers`에 정본 문서가 없다"*는 사실만 들고 **«없기로 정했다»는 기존 판정을 몰랐다**는 것이다 — 그 판정은 다른 문서(`COMPACTION.md` §8)에 살고 있었다. 교훈은 devlog 2026-08-14(코호트 검증) 결정 2가 든다.
- **U-b. 본문 절 단위 앵커** — `K-002`(문서 본문이 코드보다 낡음)의 남은 범위. 이번 설계로 K-002의 범위가 *"머리는 이제 게이트가 본다 — 남은 것은 본문"*으로 좁혀졌다.
- **U-c. `최종 개정:` 이력의 형식·길이 상한.** `CLI-INTERFACE.md`의 `최종 개정:` 이력이 8건까지 자랐고 그 길이가 F-2의 원인 중 하나였다(§3.4). 다만 잘라내는 규칙은 정보 손실을 낳으므로 지금 정하지 않는다.

  **완전성은 요구하지 않는다 — 이 필드는 선택 요약이다**(2026-08-17 확정 · `K-095` D-2). 「모든 실질 개정을 든다」를 요구하면 규약이 위의 8건짜리 이력을 스스로 강제하게 되고, 그 길이가 F-2를 낳은 자리다. 그래서 이 필드가 개정을 빠뜨린 것은 **위반이 아니며**, 머리만 읽는 독자에게 «QA 착수 후에 계약을 고쳤다»가 안 보이는 비용은 받아들인다. 그 가시성이 필요한 개정은 필드가 아니라 본문 절이 든다. 형식·길이 상한은 위 문단대로 여전히 미결이다.
- **U-d. (닫힘 — 2026-08-13) 수 서술 금지는 기계화하지 않는다.** 근거는 실측이다. `docs/` 전수에서 수 서술 형태가 **752자리**인데 2026-08-13의 여러 사이클이 실제 부패로 판정한 것은 **대략 여섯**이다 — 정밀도 1% 미만. 2026-08-12가 `packages/*/src`에서 잰 값(수 계약 절반 미만·실부패 한둘)보다 **한 자릿수 더 나쁘다**. 10사이클의 *"정밀도 낮은 격자를 늘리면 읽을 문장만 늘고 신호는 준다"* 판정이 그대로 적용되고, 이번 측정은 그 판정을 **뒤집지 않고 확인한다.**

  **대신 남는 것**: §6의 예외 명문화 · §6.1·§6.2의 두 부칙 · 사후 감사. `ARCHITECTURE.md` 머리의 자백(*"새 수치가 들어오면 다음 사후 감사까지 조용히 산다"*)은 **철회하지 않는다** — 그것은 여전히 참이고, 참인 자백을 지우는 것이 이 규약이 막으려는 부패다.

  **같은 판정이 `K-007`(한글 수사·영문 표기 손잡이 확장)도 닫는다.** 손잡이를 넓히는 것은 정확히 «자유 서술에 문자열 패턴을 하나 더 거는 일»이고, 위 752자리의 절반 이상이 한글 수사다. 넓히면 정밀도가 더 떨어진다.

  **무엇이 이 판정에 걸리지 *않는지*는 `DOC-CITATION.md` §2가 정한다** — 닫힌 구문이고 정당한 사용이 0이면 격자가 아니라 형식 검출이다. 줄번호 인용이 그 경우이고, 그래서 별도 규약으로 갈라져 기계화됐다.
- **U-e. `packages/*/test/`·`scripts/`·`plans/` 머리**는 이 규약 밖이다(§1). `test/` 머리는 `K-006`이 들고 있다.
- **U-f. (닫힘 — 2026-08-13)** §5 표 ↔ 실제 머리 대조를 기계가 하지 않는다는 구멍. **계약은 §5.1, 경계 셋은 §5.2, 구현은 같은 날 착지했다** — 순수 파서 `scripts/doc-status.mjs`의 `parseStatusTable` + `check-doc-status.mjs`의 대조. 발견 경위: 계약 테스트가 §5 표 대조와 §4의 *"발견한 `.md` 수 = 판정한 수"*를 함께 *"여기서 재지 않는 것"*으로 묶어 적었는데(`packages/cli/test/doc-status.contract.test.ts`:44) 게이트가 구현한 것은 **둘 중 뒤엣것뿐**이었다 — 한 문장이 덮인 항목과 안 덮인 항목을 묶어 들어 안 덮인 쪽이 덮인 것처럼 읽혔다.

  **`K-032`·`K-009`와 같은 계열이라던 최초 판단은 틀렸다.** 그 둘은 자유 서술에 **새 문자열 패턴**을 거는 일이라 10사이클의 *"격자를 하나 더 만들지 않는다"* 판정에 걸리지만, 이 대조의 두 피연산자는 **이미 구조적**이다 — 한쪽은 게이트가 이미 파싱하는 머리 필드이고 다른 쪽은 고정 형식의 표 행이다. §2.2가 grep 격자와 필드 파서를 가른 그 구분에서 이것은 **필드 쪽**이다. 그래서 셋을 묶지 않고 여기서 확정했다.

---

## 9. 이 문서를 적용하는 순서

**적용 완료 (2026-08-13).** 아래 세 덩어리로 실행했고 순서가 강제됐다:

1. `scripts/check-doc-status.mjs` 작성 + `pnpm check` 배선 (판정은 부작용 없는 `scripts/doc-status.mjs`로 갈렸다 — §4)
2. §5 표의 문서 머리를 전부 개정 — 이때 `DOC-STATUS.md` 자신이 `구현 완료`로 바뀌었다
3. `ARCHITECTURE.md` 머리의 금지 문구에 §6 예외 추가

**1과 2는 한 커밋이었다**(`945fe4f`). 스크립트가 먼저 착지하면 모든 문서가 ①(미선언)로 깨져 게이트가 레드가 되고, 문서가 먼저 바뀌면 검사하는 것이 없어 2026-08-13 이전 상태와 구별되지 않는다.

> **2026-08-13 정정 — 이 절과 §5가 구현 이후에도 "구현은 없다"고 말하고 있었다.** 머리는 `구현 완료`로 바뀌었는데 §5 표의 자기 행은 `구현 전`, §5 서두는 앵커가 부재한다고, 이 절은(`d2a2f96^`) *"계약만 확정했고 구현은 없다"*고 적고 있었다. 셋 다 날짜 없는 **현재형 주장**이라 §6의 과거 관측 예외에도 걸리지 않는다.
>
> **F-1(안 건드려서 낡음)이 F-1의 처방을 정의하는 문서 안에서 재발했다** — 그것도 §4가 스스로 못 본다고 자백한 자리(머리는 보고 본문은 안 본다)에서. 게이트는 그린이었고 독립 QA가 잡았다. 이 사건이 §8 U-b(`K-002`, 본문 절 단위 앵커)의 필요를 실증한다: **머리만 기계화하면 본문이 머리와 어긋나는 새 부패면이 열린다.** 근거는 `plans/20260813-doc-status-gate-execute-report.md` F-4.
