# 문서 인용 형식 규약

**`neo-agent-main/docs/*.md`가 다른 자리를 가리킬 때 무엇을 주소로 쓰는가의 정본.**

- 상태: 구현 완료
- 근거: scripts/check-doc-citation.mjs
- 작성일: 2026-08-13
- 최종 개정: 2026-08-13(신설 — 줄번호 인용 금지, 격자 판별 기준) · 2026-08-13(§3.2 — `Citation`에서 `file`을 뺀다. 순수 판정이 만들 수 없는 값이었다) · 2026-08-13(머리 — 구현 착지, 게이트가 `anchor-present`로 예고한 대로) · 2026-08-13(§3.4 — 문면 인용은 문자 그대로여야 한다(U-1), 형태를 검사 가능성 순으로 서열화, 서술적 지시를 다섯째로 명시) · 2026-08-13(§3.4 — 대조 규칙 D-1~D-6 확정, 앵커에 종료된 기록 경로 허용 · §6 U-e 원인 지목 정정 · U-f 신설) · 2026-08-13(§3.4 — 규칙 충돌 셋 해소: 경계 규칙 B-1~B-3 신설, D-2를 마커 «부류»로, D-4를 무조건 형식으로, D-5를 바깥 겹침으로 좁힘 · `~~`는 B-3 예외 · §6 U-g 신설) · 2026-08-13(§3.4 — 평문 큰따옴표도 인용부호로 확정. U-1·D-1~D-6·경계 규칙이 걸린다 · §6 U-g 해소) · 2026-08-14(§3.4 — 앵커 규칙 A-1~A-3 확정: 인용 안의 주소는 앵커 리비전에서 읽는다 · 앵커 요구는 인용부호가 아니라 지목에 걸린다 · 서술의 앵커도 실물을 요구한다 · §6 U-h 신설)

**불변(계약)**: 금지되는 인용 구문 둘(§3.2), 동결 트리 판별이 문자열 안에서 완결된다는 것(§3.1), 예시 표기의 탈출 규칙(§3.3), **인용부호가 셋이라는 것**(§3.4 — `*"…"*`·«…»·평문 큰따옴표), 문면 인용의 대조 규칙 여섯(§3.4 D-1~D-6)과 그 경계 규칙(§3.4 «경계와 내용» B-1~B-3), 앵커 규칙 셋(§3.4 «앵커가 고정하는 것» A-1~A-3).
**조정 가능(세부)**: 실패 메시지 문구, 스크립트의 파일 위치, 권장 대체 형식의 문면.

이 문서는 `DOC-STATUS.md`가 문서 **머리**에 한 일을 문서 **본문의 주소**에 한다. 둘은 대상이 다르므로 문서를 나눴다 — `DOC-STATUS.md` §1이 자기 경계를 *"머리 지위 선언"*으로 그어 두었고, 그 경계를 넓히면 §1이 거짓이 된다. 이 레포에서 그것은 정확히 F-1 계열의 부패다.

전제는 `SAFE-DEFAULTS.md` 머리와 같다: **여기 있는 어떤 것도 보안 경계가 아니다.**

---

## 1. 경계 — 이 규약이 다루는 것과 다루지 않는 것

| 다룬다 | 다루지 않는다 |
|---|---|
| `neo-agent-main/docs/*.md` 안의 **주소 표기 형식** | 인용된 **내용**이 참인가 (`K-002` — 본문 격자) |
| 대상이 존재하는가를 물을 수 있는 형태인가 | 대상이 실제로 존재하는가 (링크 검사는 이 규약 밖) |
| 우리 트리와 동결 레퍼런스 트리의 구분 | 코드(`packages/`·`scripts/`) 대상 인용 — §6 U-b |
| — | `plans/`·`devnotes/`·`kanban.md`·`devlog.md`·루트 `docs/` — §6 U-c |

**예외 목록을 두지 않는다.** `neo-agent-main/docs/` 안의 `.md`는 전부 대상이다. 근거는 `DOC-STATUS.md` §1과 같다 — 예외 목록 자체가 다음 부패 후보가 된다.

---

## 2. 왜 검사가 아니라 형식 금지인가 — 두 모집단이 정반대다

이 레포는 *"격자를 하나 더 만들지 않는다"*를 이미 판정했다(devlog 2026-08-12, 10사이클). 근거는 **정밀도**였다 — *"정밀도 낮은 격자를 늘리면 읽을 문장만 늘고 신호는 준다"*. 그 판정은 유효하고, 이 규약은 그것을 뒤집지 않는다. **뒤집는 대신 그 판정이 무엇에 대한 것이었는지를 정밀하게 만든다.**

2026-08-13 `docs/` 전수 실측 (커밋 `9387efb` 시점):

| 모집단 | 자리 수 | 어긋난 수 | 정밀도 |
|---|---|---|---|
| 우리 트리를 가리키는 **줄번호 인용** | 21 | **21** | **100%** |
| 동결 레퍼런스 트리를 가리키는 줄번호 인용 | 2 | 0 | 0% |
| **수 서술**(한글 수사 + 아라비아 수·단위) | 752 | 대략 여섯 | 1% 미만 |

**두 모집단은 같은 «격자» 물음처럼 보이지만 성질이 정반대다.**

- **수 서술**은 자유 서술에 **의미 패턴**을 거는 일이다. 걸린 것의 대부분이 정당하므로 억제 목록이 필요하고, 그 목록이 다음 부패 지점이 된다(2026-08-12 실측: `packages/*/src`에서 오탐이 절반을 넘었다 — `devnotes/20260812-devnote.md` §18.2). 10사이클이 거부한 것은 정확히 이것이다.
- **줄번호 인용**은 **닫힌 구문**이다. `.md` 뒤에 콜론과 숫자가 오는 형태이고, 우리 트리를 가리키는 정당한 사용이 **정의상 0**이다(§3에서 그렇게 정한다). 오탐이 구조적으로 없다.

**판별 기준 — 이 레포가 앞으로 «기계화할 것인가»를 물을 때 쓴다:**

> 검사 대상이 **닫힌 구문**이고 정당한 사용이 0이면 그것은 격자가 아니라 **금지된 형식의 검출**이다. 자유 서술에 의미 패턴을 거는 것만이 10사이클 판정의 대상이다.

`DOC-STATUS.md` §2.2가 필드 파서와 grep 격자를 가른 그 선의 일반형이다. 필드가 강한 이유는 그 절이 *"무엇을 읽을지 자신이 정한다"*고 적은 성질 때문인데, 닫힌 구문도 같은 성질을 갖는다 — 읽을 자리를 문자열이 스스로 표시한다.

### 2.1 100% 위반은 검사가 아니라 형식이 틀렸다는 뜻이다

적발률이 전건이면 검사를 붙여도 매번 21건을 보고하고 아무도 읽지 않는다. **전건 위반은 규율의 실패가 아니라 형식의 실패다.**

왜 전건인가 — 이 레포의 기록 규율이 직접 원인이다. *"과거 기록은 고치지 않는다"*(`CLAUDE.md`)를 지키면 문서는 **정정 주석이 위에 쌓이며 자란다.** 주석 하나가 붙을 때마다 아래의 모든 줄이 밀리고, 그 문서를 가리키던 모든 줄번호가 동시에 죽는다. 실제로 그렇게 일어났다 — 커밋 `945fe4f`가 문서 머리를 필드로 바꾸면서 열여섯 문서의 머리 아래를 한꺼번에 밀었고, **그 한 커밋이 위 자리의 절반 이상을 한꺼번에 만들었다.**

> **2026-08-13 정정 — 이 문장이 «21건 중 열다섯»이라고 적고 있었다.** 재현하니 열셋이다(`SANDBOX.md` 대상 여섯 · `ARCHITECTURE.md` 대상 다섯 · `CLI-INTERFACE.md` 대상 둘). `CLI-INTERFACE.md`가 자기 §1을 가리킨 다섯 자리는 **`945fe4f^` 시점에 이미 어긋나 있었고**, `DOC-STATUS.md`가 자기 §5를 가리킨 한 자리는 그 뒤 `92e45b3`에서 어긋난 채로 태어났으며, 동결 트리 접두가 빠진 둘은 낡음이 아니라 형식 위반이다. **고정 커밋에 대한 실측이므로 §6의 과거 관측 예외가 오기를 면제하지 않는다.** 수를 열셋으로 고치지 않고 뺀 이유는 이 문장의 논지가 **한 커밋이 동시에 죽인다**는 구조이지 다중도가 아니기 때문이다 — `DOC-STATUS.md` §6.1이 주어가 집합의 일부일 때 한정사를 남기라고 한 처방을 따랐다. 근거: `plans/20260813-doc-citation-target-verify-report.md` V-1.

**즉 줄번호 인용은 이 레포의 기록 규율과 구조적으로 양립하지 않는다.** 규율을 지킬수록 주소가 죽는다. 개인의 부주의가 아니므로 주의로는 고쳐지지 않는다.

### 2.2 동결 트리에서는 같은 형식이 살아남는다

`openclaw-main/`·`hermes-agent-main/`은 읽기 전용 스냅샷이다(`CLAUDE.md` — 수정 금지). 줄이 움직이지 않으므로 줄번호가 죽지 않고, 실측 2건이 전부 정확했다. `CLAUDE.md`가 코드 참조에 `경로:줄번호`를 요구하는 것도 이 성질 위에 서 있다.

**그래서 금지의 판별 기준은 줄번호를 쓰는가가 아니라 대상 트리가 고정돼 있는가다.** `DOC-STATUS.md` §6이 수 서술의 예외를 **시제**로 가른 것과 같은 층위의 기준이고, 다른 축이다 — 날짜만 박힌 과거형 인용은 *언제* 참이었는지는 말하지만 *지금 어디인지*는 말하지 않는다. 주소는 시제로 살아나지 않는다.

---

## 3. 계약 — 인용 형식

### 3.1 판별 기준 — 대상 트리가 고정돼 있는가

고정 수단은 둘뿐이고, 둘 다 **인용 문자열 안에서 판별된다.** 문맥을 읽어야 알 수 있는 기준은 두지 않는다 — 기계가 못 재고, 사람도 매번 다시 판단한다.

| 대상 | 고정됐나 | 줄번호 |
|---|---|---|
| `openclaw-main/…` · `hermes-agent-main/…` | 읽기 전용 스냅샷 | **쓴다** |
| `neo-agent-main/` 안의 `.md` | 정정 주석이 쌓이며 자란다 | **금지** |
| 자기 문서의 절·줄 | 같은 이유 | **금지** |

**동결 트리 인용은 트리 이름을 경로에 포함해 쓴다.** 산문에 트리 이름을 적고 백틱 안에는 파일명만 두는 형태(예: *"hermes `SECURITY.md`"* 뒤에 콜론과 숫자)는 금지다 — 판별이 문자열 밖으로 새는 순간 기계가 못 재고, 억제 목록이 필요해진다. §2가 오탐 0을 근거로 세운 계약이 바로 여기서 무너진다.

### 3.2 타입

```typescript
/**
 * 인용 한 자리. `docs/*.md` 본문 전체에서 수집된다 — 머리 40줄이 아니다.
 *
 * **문서 이름은 이 타입에 없다.** 순수 판정은 소스 텍스트만 받으므로 파일명을 원리적으로
 * 만들 수 없다 — 게이트 루프가 파일명과 짝지어 출력한다. (2026-08-13 정정: 최초 문면은
 * `file: string`을 뒀는데, `DOC-STATUS.md` §3.1이 QA 독립 검증에서 `doc: string`을 뺀 것과
 * **정확히 같은 결함**이었다. 선례를 읽고 쓴 설계가 같은 자리에서 같은 실수를 했다.)
 */
type Citation = {
  readonly text: string;   // 매치된 원문
  readonly line: number;   // 1-기반 줄 번호 — 출력용이며 판정에 쓰지 않는다
};

type CitationViolation =
  /** 트리 접두 없이 `<이름>.md` 뒤에 콜론과 숫자가 온 형태 */
  | "unpinned-doc-line"
  /** `§<절번호>` 뒤에 콜론과 숫자가 온 형태 — 자기 문서 절:줄 */
  | "self-section-line";

type CitationVerdict =
  | { readonly ok: true }
  | { readonly ok: false; readonly violation: CitationViolation; readonly detail: string };
```

**갈래를 둘로 닫는다.** 셋째 형태가 나오면 그때 늘리되, **«기타»를 두지 않는다** — 기타는 무엇이 걸렸는지 말하지 않아 위반 이름이 고칠 곳의 주소라는 성질(`DOC-STATUS.md` §5.2)을 깬다.

**허용 형식은 열거하지 않는다.** 금지만 정하고 나머지는 자유다. 허용 목록을 두면 목록에 없는 정당한 형태가 새로 생길 때마다 목록이 낡는다.

### 3.3 금지 형식을 예시로 들 때 — 숫자 자리에 `NN`을 쓴다

**이 규약은 자기 자신에게 걸린다.** 어긋난 인용을 증거로 재현하면 검사가 그 재현을 잡는다 — 이 레포가 네 번 겪은 자기오염이고(`DOC-STATUS.md` §2.2), 처방은 이미 나와 있다: *"부패했던 문자열을 **인용부호로 다시 쓰지 않는다**"*(`devnotes/20260812-devnote.md` §18.3 — 원문의 굵게 마커까지 옮긴 것은 §3.4 D-2다).

그 처방만으로는 규약을 **가르칠 수** 없으므로 탈출 표기를 둔다:

> 금지 형식을 예시로 들 때는 숫자 자리에 `NN`을 쓴다.

검사가 보는 것은 숫자이므로 `NN`은 안 걸린다. **억제 목록도 인용 블록 예외도 두지 않는다** — 파서를 관대하게 만들지 않고 쓰는 쪽이 한 글자를 바꾼다. `DOC-STATUS.md` §3.3의 *"관대한 파서는 `구현 완료(검색 제외)`도 통과시킨다"*와 같은 자리다.

### 3.4 무엇으로 대체하나 — 검사 가능성 순

**형태를 검사 가능성으로 서열화한다.** 위쪽일수록 *"이 주소가 대상을 특정하는가"*를 기계가 물을 수 있고, 아래로 갈수록 사람이 실물을 열어야만 알 수 있다. **가능한 한 위쪽을 쓴다.**

| 대신 쓸 것 | 언제 | 검사 가능성 |
|---|---|---|
| 경로 + 커밋 | 대상이 사라질 수 있다. 커밋이 트리를 고정하므로 §3.1의 «고정»을 만족한다 | **영구** — 그 시점 트리에서 언제든 재현된다 |
| 절 번호 | 대상이 절 단위로 특정된다 | 절의 실재 여부를 기계가 잰다 |
| 필드·앵커 이름 | 대상이 머리 필드다 | 필드의 실재 여부를 기계가 잰다 |
| **문면 인용** | 대상이 한 문장이다. 절이 갈려도 살아남는다 | **아래 규칙을 지킬 때만** 부분 문자열 대조가 된다 |
| 서술적 지시 | 위 넷으로 못 가리킬 때 — 예: 절 번호가 없는 문서 머리의 어느 규정 | **없음. 최후 수단** |

**첫 형태가 가장 강하다.** 커밋을 박으면 그 주소는 영구히 검증 가능하고, 대상 문장이 삭제된 뒤에도 *"그때 거기 있었다"*가 참으로 남는다. `DOC-STATUS.md` §6의 과거 관측 예외가 수에 대해 한 일을 주소에 대해 한다.

#### 문면 인용은 문자 그대로여야 한다 (2026-08-13 확정 · U-1)

> **인용부호로 감싼 문면은 대상 문서에 문자 그대로 존재하는 부분 문자열이어야 한다.** 그렇지 않은 표현을 쓰려면 **인용부호를 벗기고 서술로 쓴다** — 그것은 서술적 지시이지 문면 인용이 아니다.
>
> **인용부호는 셋이다: `*"…"*` · «…» · 평문 큰따옴표(`"…"`).** 셋 다 같은 규칙을 받는다(2026-08-13 확정 — 아래 «평문 큰따옴표» 문단).
>
> **대상 문서를 지목하지 않은 인용부호는 이 규칙 밖이다** — 강조·조어·수사는 인용이 아니기 때문이다. 다만 같은 줄에 다른 문서 이름이 있으면 읽는 사람이 인용으로 오인하므로, **조어에는 굵게(`**…**`)를 쓰고 인용부호를 아낀다.**
>
> **제거된 문면을 가리킬 때는 커밋을 함께 박는다.** 그 시점 트리에서 부분 문자열 대조가 참이므로 영구히 검증 가능하다. 커밋 없이 사라진 문면을 인용하면 아무도 그것이 있었는지 확인할 수 없다. **이 요구는 인용부호가 아니라 대상 지목에 걸린다** — 인용부호를 벗겨 서술로 바꿔도 앵커는 남는다(2026-08-14 확정 · A-2).
>
> **인용 구간 안의 주소는 앵커가 고정한 리비전에서 읽는다.** 앵커가 없으면 오늘의 대상 문서에서 읽는다(2026-08-14 확정 · A-1). 셋 다 아래 «앵커가 고정하는 것» 문단이 근거를 든다.

#### 평문 큰따옴표도 인용부호다 (2026-08-13 확정)

**세 번째 관측 만에 규칙에 들어왔다.** 최초 문면은 인용부호로 `*"…"*`와 «…» 둘만 들었고, 평문 큰따옴표가 U-1의 대상인지는 두 번의 전수 검증이 각각 지목하고도 규칙에도 미결에도 들어가지 않았다.

**판정: 대상이다.** 따라서 U-1·D-1~D-6·경계 규칙이 그대로 걸린다. 근거 셋:

1. **해악이 같고, 실측으로 확인됐다.** 2026-08-13 전수 실측에서 **대상 문서를 지목한 평문 인용 중 아홉 자리가 원문과 어긋난 채 인용부호를 달고 있었다** — 전부 의역이다(`닫는 것`을 `닫는다`로, `셸이 이미 `curl`을 칠 수 있다`를 `셸이 curl을 칠 수 있다`로). U-1이 막으려던 결함이 이 형식에서 실재한다.
2. **§2의 기준은 범위 판정용이 아니다.** 평문 큰따옴표는 조어·수사에 압도적으로 많이 쓰이므로 «정당한 사용이 0»이 아니고, 그래서 닫힌 구문이 아니다. **그러나 §2는 그 기준을 *"이 레포가 앞으로 «기계화할 것인가»를 물을 때 쓴다"*로 스스로 한정한다** — 게이트를 붙일 것인가의 기준이지 규칙이 무엇을 금지하는가의 기준이 아니다. U-1은 이미 §6 U-e에서 **기계화하지 않는 예방 규칙**으로 살기로 했으므로, 오탐 논증은 이 판정에 걸리지 않는다.
3. **형식으로 가르면 도피처가 생긴다.** 평문을 규칙 밖에 두면 **평문으로 쓰면 규칙 밖**이라는 도피처가 성립한다 — D-6이 닫은 것과 같은 구조다.

**오탐은 이미 있는 예외가 처리한다.** 위 문단의 *"대상 문서를 지목하지 않은 인용부호는 이 규칙 밖이다"*가 조어·수사·가상 발화를 전부 규칙 밖에 둔다. 실측에서 평문 큰따옴표 대부분이 여기 들고, 감사의 실질 모집단은 **대상 문서를 지목한 줄**뿐이다.

**소급 비용은 U-1 아홉 자리뿐이다.** 같은 실측에서 평문 인용의 **바깥 겹침(D-5) 0건 · 끝 문장 부호(D-4) 0건**이 확인됐다 — 이 판정은 경계 규칙의 처분을 늘리지 않는다. 지목한 평문 인용 중 문자 그대로 일치하는 자리도 상당수 있고, **그것들은 적합이므로 형식을 승격시키지 않는다** — U-1이 묻는 것은 표기가 아니라 대조가 참인가다.

**여전히 조어에는 굵게를 쓴다.** 위 문단의 *"조어에는 굵게(`**…**`)를 쓰고 인용부호를 아낀다"*는 권고이고 이 판정이 그것을 강화한다 — 이제 평문 큰따옴표도 대상 문서를 지목하면 인용으로 읽히기 때문이다. **권고이므로 소급하지 않는다.**

#### 무엇이 «문자 그대로»인가 — 대조 규칙 (2026-08-13 확정)

위 세 문단은 *"문자 그대로"*를 요구하면서 **무엇과 대조하는지**를 정하지 않았다. 하루 뒤 전수 소급에서 그 틈이 전부 드러났으므로(`plans/20260813-docs-verbatim-quote-verify-report.md` §4) 여기서 닫는다. **아래 여섯이 이 규칙의 대조 절차다.**

| # | 규칙 | 왜 |
|---|---|---|
| **D-1** | **대조 단위는 대상의 «논리 단위»다** — 마크다운 표는 **한 칸**, 코드 주석은 **한 줄**, 그 밖은 **한 문단**. 인용은 그 안에서 끊는다 | 파일 원문에는 문장이 아닌 잡음이 섞인다. 표의 칸 구분자(`\|`)와 주석 거터가 그것이다. 단위를 안 정하면 «부분 문자열»이 어디의 부분인지 매번 갈린다 |
| **D-2** | **정규화는 공백만.** 줄바꿈과 연속 공백을 하나로 본다. 그 밖에는 **원문 문자를 그대로 옮긴다** — 원문 안의 **인라인 마크다운 마커**(굵게·기울임·백틱·취소선…)도 함께. **목록이 아니라 부류다.** 단 경계에 걸친 마커는 아래 «경계와 내용»이 처분한다 | 이 레포의 문서는 줄바꿈 위치가 자주 바뀌지만 문자는 안 바뀐다. 반대로 마커를 지우면 대조가 실패하고, 실제로 그렇게 실패한 자리가 있었다. **부류로 쓰는 이유**는 대조 상대가 파일의 raw 텍스트이기 때문이다 — raw에 있는 마커는 종류를 가리지 않고 걸리므로, 열거하면 다음 마커가 나올 때 목록이 낡는다(§1의 예외 목록 금지와 같은 자리) |
| **D-3** | **생략을 인용부호 안에 표시하지 않는다.** 중간을 건너뛰려면 **인용을 둘로 나눈다** | 생략 기호가 들어간 문자열은 어느 대상에도 없다. 표시한 생략은 정직하지만 **검사 가능성을 0으로 만든다** — §3.4 표가 문면 인용을 절 번호보다 위에 둔 이유가 사라진다 |
| **D-4** | **인용 구간의 첫 글자와 마지막 글자는 문장 부호가 아니다**(`.` `,` `:` `;` `?` `!` `…`). **원문에 그 부호가 있어도 인용부호 안에 넣지 않는다.** 구간 **안쪽**의 문장 부호는 대상이 아니다 — 경계만 본다 | **판정이 문자열 안에서 끝나는 유일한 방향이기 때문이다.** 조건부로 읽으면(«원문에 없을 때만 금지») 인용 끝의 부호가 전부 **원문을 열어야 판정되는 후보**가 되고, 그것이 §2가 거부한 오탐 구조다. 반대로 무조건 금지는 **U-1을 절대 깨지 않는다** — 접미 한 글자를 떼도 부분 문자열 성질은 보존된다. 즉 «싸고 안전한 쪽»이 무조건 형식이다 |
| **D-5** | **인용부호로 감싼 문면을 다시 강조 마커로 감싸지 않는다** — `` `**"…"**` ``·`` `**«…»**` `` 양쪽 다. **안쪽** 마커는 D-5가 아니라 D-2와 «경계와 내용»이 처분한다 | 겹친 표기 안에는 인용 표기가 문자열로 들어 있어 **기계가 둘을 구별할 수 없다**. 바깥 강조는 인용하는 쪽이 자기 강조를 덧씌운 것이라 원문에 대해 아무것도 주장하지 않으므로, 벗겨도 U-1이 잃는 것이 없다 |
| **D-6** | **이 규칙이 걸리는 기준은 «인용이 쓰인 자리»다** — 대상이 어디든 상관없다. `docs/*.md` 안에 쓴 인용이면 대상이 코드든 기록이든 걸린다 | §1이 경계 밖에 둔 것은 **그 파일들 안에 쓰인 인용**이지 그 파일들을 **가리키는** 인용이 아니다. 둘을 안 가르면 `plans/`를 인용하면 규칙 밖이라는 도피처가 생긴다 |

**판정 층.** 위 여섯은 두 부류다 — **형식**(판정이 인용 문자열 안에서 끝난다: D-3·D-4·D-5)과 **대조**(대상 원문을 열어야 판정된다: D-1·D-2). D-6은 어느 쪽도 아니고 나머지의 적용 범위를 정한다. 이 구분이 §6 U-e의 입력이다 — 기계화는 형식 쪽부터만 가능하다.

##### 경계와 내용 — 규칙 충돌 셋의 공통 처방 (2026-08-13 확정)

**인용은 두 층이다: «어디서 끊는가»(구간 선택)와 «구간 안에 무엇이 오는가»(구간 내용).** D-1·D-4가 앞을 정하고 D-2·D-3이 뒤를 정한다. 2026-08-13에 올라온 충돌 셋은 전부 **경계에 걸친 것을 내용 규칙으로 처분하려다 난 것**이었다. 그래서 경계를 먼저 닫는다.

> **경계 규칙: 인용 구간의 양 끝에는 «내용 문자»만 온다.** 마커도 문장 부호도 경계에 두지 않는다.
>
> - **B-1.** 첫·마지막 글자가 문장 부호이면 안 된다 → **D-4**.
> - **B-2.** 인용 구간이 인라인 마커 쌍의 **한쪽만** 담으면 안 된다. 쌍을 온전히 담거나 온전히 밖에 둔다.
> - **B-3.** 마커 쌍이 인용 구간을 **정확히** 감싸면 그 쌍은 **인용 밖이다** — 벗기고 인용부호만 남긴다.

**B-3이 D-2와 D-5의 충돌을 해소한다.** 원문이 `` `**X**` ``이고 인용하려는 것이 `X` 전체일 때, D-2를 문자대로 따르면 표기가 `` `*"**X**"*` ``가 되어 D-5에 걸린다. 어느 규칙을 이기게 할지는 임의 선택으로 보였으나 **임의가 아니다** — 정확히 감싼 쌍은 **벗겨도 U-1이 안 깨진다**(`X`는 `` `**X**` ``의 부분 문자열이다). D-2가 지키려던 것이 부분 문자열 대조이므로, 이 경우에 한해 D-2는 **잃는 것이 없다.** 반대로 마커가 인용의 **진부분**이면(`` `A **B** C` ``) 벗기는 순간 대조가 깨지므로 그대로 옮긴다 — **이 자리는 어떤 검사도 반드시 통과시켜야 한다.**

**그래서 «겹침에 방향이 있는가»라는 물음은 답이 아니라 해소된다.** 바깥 겹침(`` `**"X"**` ``)은 D-5가, 안쪽 겹침(`` `*"**X**"*` ``)은 B-3이 처분한다. **둘 다 금지되지만 근거가 다르다** — 바깥은 인용하는 쪽의 덧씌운 강조라 원문에 대해 아무 주장도 하지 않고, 안쪽은 «원문에 이 마커가 있다»는 주장이라 D-2·U-1의 관할이다. 안쪽 형태가 원문에 근거 없이 쓰였다면 그것은 D-5가 아니라 **U-1 위반**이다.

**D-5는 인용부호 셋 모두에 걸린다.** §3.4가 형식들을 **동등하게** 들었으므로 하나만 걸면 «다른 형식을 쓰면 된다»는 도피처가 생긴다 — D-6이 *"`plans/`를 인용하면 규칙 밖이라는 도피처가 생긴다"*로 닫은 것과 같은 근거다. 실물은 «…»·평문 둘 다 0건이라 소급 비용도 0이다.

**취소선(`~~`)은 B-3의 예외다 — 벗기지 않는다.** 굵게·백틱은 **표시**지만 취소선은 **내용**이다(이 문면은 더 이상 유효하지 않다). 벗기면 인용이 **지금 유효한 문장**이라는 거짓 신호를 준다. 취소선이 인용 구간을 정확히 감싸면 **그 문면을 인용부호로 인용하지 않는다.** 대신 둘 중 하나를 쓴다 — ① 철회 **전** 커밋을 앵커로 박고 그 시점 문면을 인용한다(그 시점에는 취소선이 없다), ② 인용을 포기하고 산문으로 «철회된 문면»임을 밝힌다. ①이 이미 이 레포의 실물 선례다: `DISTRIBUTION.md`가 `TECH-STACK.md`의 유예 항목을 인용하며 `6d9019e^`를 박았고, 오늘 그 항목은 취소선이 걸려 있다.

**제거된 문면의 앵커는 커밋 또는 종료된 기록의 경로다.** 커밋만으로 한정하지 않는 근거는 §6 U-c다 — `plans/`·`devnotes/`는 종료 후 고정되므로 그 경로에서의 대조도 커밋과 같은 성질을 갖는다. **어느 쪽이든 앵커에서 실제로 대조가 참이어야 한다**: 커밋이면 `git show <앵커>:<경로>`에, 기록이면 그 파일에 문면이 있어야 한다. 지운 커밋을 앵커로 쓸 때 그 **부모**(`<해시>^`)를 쓰는 것은 그래서다. **앵커를 박았다는 사실이 대조를 대신하지 않는다.**

**조어를 굵게로 쓰라는 위 문단은 권고이지 위반 판정 기준이 아니다.** 조어인지 인용인지는 의미 판정이라 오탐이 필연이고, §2가 거부한 바로 그 구조가 된다. 다만 **D-5가 금지하는 표기 자체는 형식이므로 위반이다** — 판정이 문자열 안에서 끝난다.

> **2026-08-13 개정 — 한 칸에는 한 술어만 둔다.** 개정 전 D-4와 D-5는 **둘 다** 무조건 형식 문장과 조건부·권고 문장을 한 칸에 담고 있었다(`dbdd55c` — D-4: *"문장 부호는 인용부호 밖에 둔다"* + *"원문에 없는 마침표를 안에 넣지 않는다"*, D-5: *"조어에 인용부호를 겹쳐 쓰지 않는다"* + 겹친 표기 금지). 넓게 읽는가 좁게 읽는가에 따라 위반 수가 갈렸고, **그것이 이번에 올라온 규칙 충돌 셋의 공통 원인이다.** 개정 후 두 칸은 형식 술어 하나씩만 들고, 권고 갈래는 이 문단과 위 문단이 산문으로 갖는다. 근거: `plans/20260813-docs-quote-independent-verify-report.md` J-1·J-2 · `plans/20260813-quote-anchor-disposition-qa-report.md` G-1·G-2·G-5.
>
> ***한 규칙 안에 무조건 문장과 조건부 문장이 함께 있으면 그것은 두 규칙이다.*** 표의 한 칸이 두 술어를 담으면 위반 집합이 독자마다 달라지고, 그 차이는 소급 감사가 실제로 자리를 셀 때까지 드러나지 않는다.

**금지 형식과 표기 자체를 예시로 들 때는 이 규칙 밖이다.** §3.3이 숫자에 대해 둔 탈출과 같은 자리다 — 형식을 가르치려면 그 형식을 적어야 한다.

**왜 이 규칙인가.** 문면 인용은 *"절이 갈려도 살아남는다"*는 이유로 대체 형식에 들어왔는데, **의역·축약을 허용하면 그 장점이 사라진다** — 대상 문서를 아무리 뒤져도 그 문자열이 없으므로 절 번호보다 나을 것이 없고, 인용부호가 *"원문이 이렇다"*고 거짓 신호를 준다.

발견 경위: `CLI-INTERFACE.md`가 §1을 가리키며 인용부호 안에 **스크립트**라고 적었는데, §1의 원문은 *"수치의 정본은 `scripts/check-core-budget.mjs`이고"*로 파일 이름을 든다. 대상 특정에는 성공했으나 인용부호가 원문에 없는 낱말을 담고 있었다(`plans/20260813-doc-citation-target-verify-report.md` U-1).

**이 규칙은 기계화하지 않는다 — 지금은.** §2의 판별 기준을 자기 자신에게 적용한 결과다. 2026-08-13 실측: `docs/` 교차 문서 인용 48자리 중 26이 이 규칙에 걸리는데, **그중 상당수가 오탐**이다 — 한 줄에 여러 문서 이름과 여러 인용이 섞이면 *"어느 문서를 인용한 것인가"*를 사람이 판단해야 하고, 자기 조어를 인용부호로 감싼 자리가 다른 문서 이름과 같은 줄에 있으면 그대로 걸린다. **정당한 사용이 0이 아니므로 닫힌 구문이 아니고, 억제 목록이 필요해지는 순간 §2가 거부한 그 구조가 된다.**

즉 이것은 **예방 규칙**이고, `ARCHITECTURE.md` 머리의 수 서술 금지와 같은 자리에 선다 — 검사 수단 없이 사는 규칙이라는 것을 여기 적어 둔다(`DOC-STATUS.md` §2 표의 F-3이 그런 규칙이 어떻게 깨지는지 보여준다). **재도입 트리거는 §6 U-e.**

#### 앵커가 고정하는 것 — A-1·A-2·A-3 (2026-08-14 확정)

위 문단들은 앵커를 «제거된 문면이 실재하는 자리»로만 썼고, 앵커가 **그 밖에** 무엇을 고정하는지는 정하지 않았다. 2026-08-14 재처분 사후 검증이 그 틈으로 갈리는 자리 둘을 실물로 냈다(`plans/20260814-qa-redisposition-verify-report.md` J-1·J-2). 둘 다 같은 미규정에서 나왔으므로 여기서 함께 닫는다.

| # | 규칙 | 왜 |
|---|---|---|
| **A-1** | **인용 구간 «안»의 주소는 앵커가 고정한 리비전에서 읽는다** — 커밋 앵커면 그 트리, 종료된 기록 경로 앵커면 그 파일. 앵커가 없으면 오늘의 대상 문서에서 읽는다. **주소는 부류다** — 절 번호·문서 이름·경로·필드 이름·커밋 해시를 가리지 않는다. **인용 안의 주소가 오늘 풀리지 않는 것은 이 규약의 위반이 아니다** | 인용은 과거 산출물의 **재현**이지 인용하는 쪽의 주장이 아니다. 안의 주소는 대상 트리의 화석이므로 그 트리의 시점에서 읽어야 뜻이 선다. 오늘의 주소로 읽으면 **고칠 방법이 없는 위반**이 된다 — 이름을 보태거나 번호를 갱신하는 순간 U-1이 깨지기 때문이다. 도피처가 되지도 않는다: 안에 무엇이 오는지는 U-1이 이미 앵커 시점 원문으로 묶어 두었으므로 **날조할 수 없다.** §3.4 표가 «경로 + 커밋»의 검사 가능성을 *"그 시점 트리에서 언제든 재현된다"*로 최상위에 둔 근거가 정확히 이 성질이다 |
| **A-2** | **앵커 요구는 인용부호가 아니라 «제거된 문면을 가리킨다»는 사실에 걸린다.** 인용부호를 벗겨 서술로 바꿔도(①) 그 문장이 여전히 제거된 문면을 가리키면 앵커는 남긴다 | 위 «제거된 문면을 가리킬 때는 커밋을 함께 박는다» 문단이 든 해악은 *"아무도 그것이 있었는지 확인할 수 없다"*이고, **그 해악은 인용부호를 벗겨도 한 글자도 줄지 않는다.** 조항을 인용부호에 걸면 ①이 그대로 도피처가 된다 — D-6이 *"`plans/`를 인용하면 규칙 밖이라는 도피처가 생긴다"*로 닫은 것과 같은 구조이고, ①은 U-1 위반의 **표준** 탈출구라 이 도피처가 가장 많이 밟힐 자리다 |
| **A-3** | **서술의 앵커도 실물을 요구한다** — 그 리비전에서 서술이 가리키는 문면을 사람이 찾을 수 있어야 한다. 앵커는 «틀린 인용이 실려 있던 리비전»이 아니라 **«가리키는 원문이 실재하는 리비전»**이다 | «앵커를 박았다는 사실이 대조를 대신하지 않는다»의 서술판이다. 서술은 의역이므로 문자열 대조가 없고, 남는 요건은 «열 실물이 특정되고 거기 있는가»뿐이다. 이것마저 빼면 앵커가 장식이 된다. 가리키는 원문이 **어느 리비전에도 없으면** 앵커를 찾을 일이 아니라 **그 서술이 거짓이므로 문장을 지운다** |

**앵커는 «어디를 열 것인가»를 정하고, 인용은 «열어서 무엇을 대조할 것인가»를 정한다.** 셋을 한 문장으로 줄이면 이것이다. ①은 뒤쪽을 포기하는 처분이지 앞쪽까지 포기하는 처분이 아니다 — §3.4 표가 서술적 지시의 검사 가능성을 *"없음. 최후 수단"*으로 적은 것은 **그 형태 단독의 상한**이지 앵커를 버리라는 처방이 아니다.

**표는 고치지 않는다.** ①에 앵커를 붙인 것은 표 첫 행(«경로 + 커밋»)과 마지막 행의 **결합**이고, 결합의 검사 가능성을 표에 적기 시작하면 행이 조합 수만큼 늘어난다. 표는 **형태 단독**을 재고 결합은 이 문단이 든다.

**판정 층.** 위 «판정 층» 문단의 분류로 **A-1은 형식도 대조도 아니다** — 판정 대상을 **빼는** 해석 규칙이다. **A-2는 범위**로 D-6과 같은 층이고, **A-3은 대조**다(실물을 열어야 판정된다). §6 U-e의 입력으로 볼 때 A-2는 나쁜 소식이다 — 모집단에 **인용부호가 없는 문장**이 들어오므로, 기계는 귀속에 더해 **이 문장이 문면을 가리키는가**까지 문자열 안에서 물어야 한다.

**오늘의 주소를 인용 밖에 병기하는 것은 자유이나 의무가 아니다.** 병기한 주소는 그 자체로 이 규약의 대상이 되어 **낡고**, A-1이 이미 읽는 법을 정했으므로 의무로 걸어 얻는 것이 없다. 인용 안의 주소가 **앵커 시점에도** 안 풀리면 그것은 대상 문서 쪽의 부패이지 인용하는 쪽의 위반이 아니다 — `K-002`의 자리다.

**①과 ②(원문 복원) 중 무엇을 고르는가는 여기서 정하지 않는다.** 처분 형식의 선택은 §6 U-d의 범위다. A-1~A-3은 **어느 쪽을 골랐든** 앵커에 걸리는 요건만 정한다.

**소급 폭은 재지 않았다.** A-2가 걸리는 모집단은 앵커를 **버린** 자리만이 아니라 애초에 앵커 **없이** 쓰인 ① 자리 전부다. 2026-08-14 검증이 실물로 든 것은 `TOOLS-INTERFACE.md`의 한 자리이고(`plans/20260814-qa-redisposition-verify-report.md` J-2), 나머지는 세지 않았다 — 전수 소급은 별도 사이클이다. **A-1은 정의상 소급 처분을 만들지 않는다**: 위반을 늘리는 것이 아니라 읽는 법을 정하기 때문이다.

---

## 4. 게이트 — 무엇을 검사하는가

- 자리: **`neo-agent-main/scripts/check-doc-citation.mjs`**(실행부) + 순수 판정 `scripts/doc-citation.mjs`. `pnpm check`가 부른다.
- **판정과 실행부를 파일로 가른다.** 근거는 `DOC-STATUS.md` §4와 같다 — 한 파일이면 계약 테스트가 판정 함수를 임포트하는 것만으로 `process.exit(1)`이 테스트 워커를 죽인다.
- **`check-doc-status.mjs`에 합치지 않는다.** 그쪽의 대상은 머리 40줄이고 이것은 본문 전체다. 실패 메시지가 섞이면 *"무엇이 깨졌나"*가 흐려진다 — 예산 게이트를 분리한 것과 같은 근거.
- 탐색 범위는 **파일 전체**다. 인용은 본문에 살고, 머리 규약과 달리 자리를 좁힐 수 없다. 좁힐 수 없다는 것이 §2의 «닫힌 구문»에 기댄 이유다 — 범위로 못 줄이는 대신 구문으로 줄인다.
- **판정되지 않은 문서는 통과가 아니다.** 발견한 `.md` 수와 판정한 수가 다르면 그 자체로 실패한다(`DOC-STATUS.md` §4와 같은 fail-closed).
- 출력은 위반마다 **문서 · 위반 이름 · 원문**. 성공도 조용하지 않다(`ARCHITECTURE.md` §2.6).

**게이트가 잡지 못하는 것.** 형식만 본다 — 절 번호가 실재하는 절인지, 문면 인용이 실제 문장과 같은지는 묻지 않는다. 즉 이 게이트는 **주소가 죽는 형식**을 막지 **죽은 주소**를 잡지 않는다. 후자는 `K-002`(본문 절 단위 앵커)의 자리다.

---

## 5. 레퍼런스 대비 — 형식 회피가 선례다

| 항목 | 판정 | 근거 |
|---|---|---|
| 두 레퍼런스의 문서 안 줄번호 인용 | **선례 없음** | `openclaw-main/docs`·`hermes-agent-main`의 마크다운에서 이 형태 **0건**(2026-08-13 실측). 둘 다 검사로 풀지 않고 **애초에 쓰지 않는다** — 이 규약이 도달한 결론과 같다 |
| OpenClaw `scripts/docs-link-audit.mjs` | **흔적만** 🧬 | 878줄. 링크·라우트·앵커가 **닿는가**를 검사한다. 자리(문서 참조를 CI에서 잰다)만 가져오고 대상은 다르다 — 그쪽은 발행 사이트(Mintlify)의 링크 무결성이고 우리는 주소 형식이다. 재도입 트리거: 죽은 절 번호가 실제 부패로 관측될 때 |
| OpenClaw `scripts/check-docs-mdx.mjs` | **안 함** | MDX 문법 검증. 우리는 순수 마크다운이라 대상이 없다 |
| hermes `AGENTS.md` | **없음** | 75KB 개발 규율 문서에 인용 형식 규정이 없다. grep 무소득 |

**선례가 «검사»가 아니라 «회피»라는 것이 이 설계의 확인이다.** 두 프로젝트 모두 줄번호를 안 쓰므로 검사할 대상이 없었다. 우리는 이미 21자리를 썼으므로 회피만으로는 부족하고, 그래서 형식 금지 + 검출을 함께 둔다.

---

## 6. 미결 — 이 문서가 정하지 않은 것

- **U-a. 죽은 주소 자체의 검사.** §4가 밝힌 대로 이 게이트는 형식만 본다. 절 번호가 실재하는지는 `K-002`의 범위이고 수단은 미설계다.
- **U-b. 코드(`packages/`·`scripts/`) 대상 줄번호 인용.** 이 규약 밖이다. 근거는 실측이다 — `docs/`에서 코드를 가리킨 줄번호 인용 셋이 **셋 다 정확했다**(2026-08-13, 커밋 `9387efb`). 코드는 정정 주석이 쌓이며 자라지 않으므로 §2.1의 원인이 걸리지 않는다. 표본이 작으므로 재도입 트리거를 둔다: **코드 대상 줄번호 인용이 한 건이라도 어긋난 것으로 관측되면 이 규약의 범위를 코드까지 넓힌다.**
- **U-c. `plans/`·`devnotes/`·`kanban.md`·`devlog.md`의 인용.** 강제 범위 밖이고 권고만 한다. `plans/`·`devnotes/`는 종료 후 고정되는 기록이라 대상이 움직여도 그때의 주소로서 유효하다. **`kanban.md`는 다르다** — 유일하게 제자리 수정되는 기록이고 카드가 현재를 가리키므로 죽은 주소가 다음 세션을 잘못된 파일로 보낸다. 실제로 그렇게 됐다(2026-08-13 관측: 카드 셋이 `ARCHITECTURE.md`의 수 서술 금지를 죽은 줄번호로 가리키고 있었고, 그중 둘은 존재하지 않는 절 번호를 다음 명령에 들고 있었다). 게이트를 `docs/` 밖으로 넓힐 것인지가 판정 항목이다.
- **U-e. 문면 인용 규칙의 기계화 (§3.4).** 지금은 예방 규칙이다 — 2026-08-13 실측에서 오탐이 걸러지지 않아 §2의 «닫힌 구문» 조건을 만족하지 못했다.

  > **2026-08-13 개정 — 트리거는 발동했고, 선행 과제 둘 중 하나가 남았다.**
  >
  > **트리거는 발동했다.** 원래 문면은 *"인용부호 안의 문면이 대상 문서에 없어서 생긴 부패가 한 건이라도 관측될 때"*(`2607a1b`)였고, `docs/` 전수 소급이 그 관측을 냈다(`plans/20260813-docs-verbatim-quote-verify-report.md` §3.1). **그럼에도 아직 기계화하지 않는다** — 그 문면은 미탐 비용만 말하고, 오탐이 신호를 덮는지는 아래 선행 과제가 답한다.
  >
  > **선행 과제 ②의 원인 지목이 틀렸다.** 개정 전 문면은 이 자리의 실패 원인을 **줄바꿈**이라 적었으나, 공백을 정규화해도 대조가 실패한다. 실제 원인은 인용한 쪽이 **원문 안의 인라인 마크다운 마커를 지운 것**이다. §3.4 D-2가 그 층위를, D-1이 코드 주석 거터와 표 칸 구분자를 함께 닫았다 — **②는 해소됐다.**
  >
  > **남은 것은 ① «어느 문서를 인용한 것인가»뿐이다.** D-5(조어에 인용부호를 겹치지 않는다)가 오탐의 한 갈래를 줄였으나, 한 줄에 문서 이름이 둘 이상이면 귀속은 여전히 사람이 판단한다. **재도입 조건을 이것으로 좁힌다: 대상 귀속이 문자열 안에서 판정 가능해질 때.**
  >
  > **2026-08-13 추가 — 규칙을 형식/대조로 갈랐으나 귀속 문제가 형식 쪽까지 막는다.** §3.4의 «판정 층»이 D-3·D-4·D-5를 형식으로 분류했고, 그중 D-4는 원문을 열지 않고 인용 문자열만 봐도 판정된다 — 여기까지는 기계화의 전제가 선다. **그럼에도 못 건다**: §3.4 예외가 조어·수사 인용을 규칙 밖에 두므로, 인용 끝 부호를 전수로 잡으면 대상 문서를 지목하지 않은 인용(예: 사용자 발화를 흉내 낸 수사)이 그대로 걸린다. **즉 선행 과제 ①은 대조 규칙만이 아니라 형식 규칙의 기계화도 막고 있다** — 재도입 조건은 그대로다.
  >
  > **2026-08-14 추가 — A-2가 모집단에 인용부호 없는 문장을 넣었다.** §3.4 A-2는 앵커 요구를 대상 지목에 걸므로, 기계화하려면 인용부호라는 **출발 신호조차 없는** 문장을 대상으로 삼아야 한다. 재도입 조건은 그대로이나 **난도가 한 단계 올라갔다** — 귀속 판정에 더해 **이 문장이 문면을 가리키는가**가 선행한다. A-1은 반대 방향이다: 인용 안의 주소를 판정 대상에서 빼므로 기계화의 부담을 줄인다.
  >
  > **기계화한다면 착지 언어는 `.mjs`다** — `TECH-STACK.md` §1이고 게이트 셋이 전부 그렇다. 2026-08-13 사이클이 쓴 대조 하니스는 Python 프로토타입이라 그대로 게이트가 되지 않는다(`devnotes/20260813-devnote.md` §41).

- **U-f. `docs/`의 근거가 공개 레포 안에서 닫히지 않는다.** §3.4가 제거된 문면의 앵커로 «종료된 기록의 경로»를 허용했는데, `plans/`·`devnotes/`는 공개 레포에서 추적되지 않는다(루트 `.gitignore`). 커밋 앵커는 `docs/`가 추적되므로 공개 레포만 가진 독자도 대조할 수 있지만, 기록 앵커는 그렇지 않다. **이 규약이 만든 문제가 아니다** — `docs/*.md`가 기록을 근거로 드는 것은 이미 널리 쓰이는 형태이고(2026-08-13 실측: 39줄), 규약이 그 관행을 승인했을 뿐이다. 판정 항목은 **공개 산출물의 근거가 비공개에 있어도 되는가**이고, 그것은 이 문서보다 큰 결정이다. **트리거: 공개 레포만 가진 독자를 상정해야 할 일이 생길 때**(외부 공개·기여자 유입).
- **U-d. 기존 21자리의 처분 형식.** §3.4가 대안 넷을 주지만 자리마다 어느 것이 맞는지는 정하지 않았다. 절 제목에 든 인용은 특히 갈린다 — 제목을 바꾸면 그 절을 가리키던 인용이 함께 죽는다. **2026-08-14 추가**: ①(서술 전환)과 ②(원문 복원) 중 무엇을 고르는가도 이 미결의 범위다. §3.4 A-1~A-3은 선택을 정하지 않고 어느 쪽에든 걸리는 앵커 요건만 정했다.
- **U-h. A-2의 «제거된 문면을 가리킨다»가 무엇인가.** A-2가 요건을 정했으나 **어떤 서술이 문면을 가리키는지**는 의미 판정이다. 가르는 예로는 — 대상 문서를 지목하며 **원래는 …라고 적혀 있었다**는 안, 문면이 아니라 동작·결정·사실만 서술하는 것은 밖. **예시이지 규칙이 아닌 이유는 둘 사이가 연속이기 때문이다.** 이 미결은 독립적이지 않다: §3.4의 *"대상 문서를 지목하지 않은 인용부호는 이 규칙 밖이다"*가 인용에 대해 그은 선과 **같은 «지목» 축**이고, 그 선의 경계가 이미 열려 있다(`K-066`). 즉 `K-066`이 지목을 정하면 A-2의 모집단도 함께 정해지므로 **둘을 한 설계에서 보는 것이 맞다.** 그때까지 A-2는 명백한 자리(제거된 문면을 지목하는 서술)에만 적용하고 경계 사례는 판정으로 올린다.

---

## 7. 이 문서를 적용하는 순서

**적용 전.** 순서가 강제된다:

1. `scripts/doc-citation.mjs`(순수 판정) + `scripts/check-doc-citation.mjs`(실행부) 작성, `pnpm check` 배선
2. 기존 21자리를 §3.4의 형식으로 처분
3. `kanban.md`의 죽은 인용 처분(U-c의 권고 범위 — 게이트 밖이므로 손으로)

**1과 2는 한 커밋이어야 한다.** 스크립트가 먼저 착지하면 21자리가 전부 위반이라 게이트가 레드가 되고, 처분이 먼저면 검사하는 것이 없어 다음 인용이 같은 형식으로 다시 들어온다. `DOC-STATUS.md` §9가 같은 이유로 같은 순서를 강제했다.
