# 참조 소스트리 — 읽기 전용 타사 코드 스냅샷 (2026-08-05). 이력에 넣지 않는다
openclaw-main/
hermes-agent-main/
# 2026-09-09 추가 — 새 참조 트리가 무시 목록에 없으면 `git add -A` 한 번에 공개 레포로
# 나간다. 이 자리는 트리가 늘 때마다 손으로 따라와야 하는 목록이다
openclaw-2.0-main/

# OS
.DS_Store

# Node
node_modules/
*.log

# 로컬 전용 설정·스킬
/.claude/
# Codex 작업 규약 — CLAUDE.md와 같은 부류(하네스). 비공개 백업 대상
/AGENTS.md
# Codex 작업 자리 — 디자인 시스템 정본(2026-09-10 승격. 그전엔 원격의 파생 미러)·프로젝트 전용
# 스킬 이식본·역할 지시서. .claude/와 같은 부류(하네스)라 공개하지 않고 비공개 백업 대상이다
# (backup-records.sh가 agents-tools/로 미러링. 2026-09-07의 «백업하지 않는다»는 이 날 뒤집혔다)
/.agents/

# 내부 작업 문서 — 로컬에만 둔다. 공개 레포에는 neo-agent-main/만 올린다
# (neo-agent-main/docs/는 매칭되지 않도록 루트 경로만 지정)
/CLAUDE.md
/devlog.md
/devnotes/
/kanban.md
/backlog.md
/idea.md
/MILESTONE.md
/milestones/
/docs/
/plans/
